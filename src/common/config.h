// sinvr.cfg -- runtime settings, read from beside SinEpisodes.exe.
//
// This exists because the machine that builds this mod and the machine that has
// the headset are not the same machine. Anything that might need adjusting on
// the VR rig belongs here, so it can be changed and retested there without a
// rebuild-and-copy round trip.
//
// The file is written with documenting comments on first run.
#pragma once

#include <windows.h>
#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <vector>

namespace sinvr {

class Config
{
public:
	// Keys added after a user's sinvr.cfg was first written. Without this, an
	// older file silently keeps every new setting at its built-in default --
	// which is exactly how eye_adjust_enabled stayed off despite being set.
	static const char* const* KnownKeys( size_t& count )
	{
		static const char* const keys[] = {
			"log_level", "log_heartbeat_seconds", "vr_enabled", "world_scale",
			"apply_roll", "pitch_limit", "stereo",
			"positional_tracking", "positional_scale", "positional_max_offset", "aim_convergence_distance",
			"aim_convergence_trace", "aim_convergence_max",
			"aim_bore_right", "aim_bore_up", "aim_bore_fwd", "aim_bore_adjust", "aim_bore_step",
			"aim_bore_yaw", "aim_bore_pitch", "aim_bore_angle_step", "aim_origin_head",
			"controller_input", "controller_move_deadzone", "controller_turn_deadzone",
			"controller_snap_turn", "controller_snap_degrees",
			"turn_stick_crouch", "turn_stick_grenade", "turn_stick_threshold", "off_hand_grenade",
			"menu_double_click", "menu_double_click_ms",
			"use_trace", "use_aim_head", "use_aim_tail",
			"matsys_vtable_dump", "hud_material_trace",
			"controller_smooth_turn_speed", "movement_direction", "left_handed",
			"swap_thumbsticks",
			"aim_source", "hide_crosshair_when_aiming", "recoil_compensation",
			"viewmodel_follow_controller", "viewmodel_offset_forward",
			"viewmodel_adjust_enabled", "viewmodel_angle_pitch",
			"viewmodel_angle_yaw", "viewmodel_angle_roll",
			"viewmodel_offset_right", "viewmodel_offset_up",
			"stereo_projection", "viewmodel_fov_match", "viewmodel_depth", "menu_gating", "eye_separation_scale", "submit_frames",
			"menu_convergence", "menu_scale", "menu_pointer", "menu_pointer_off_hand",
			"menu_pointer_click", "menu_pointer_marker", "menu_pointer_marker_distance",
			"menu_pointer_marker_size", "menu_anchor", "menu_anchor_distance",
			"menu_anchor_yaw", "menu_anchor_height", "menu_world_panel",
			"menu_panel_width", "menu_panel_billboard", "menu_panel_align_scene",
			"menu_panel_repin_ms",
			"menu_panel_pitch", "menu_panel_roll", "menu_panel_adjust",
			"menu_depth_scale", "menu_depth_step", "menu_draw_probe", "menu_depth_fix" "menu_depth_bias",
			"menu_pointer_screen_marker",
			"menu_pointer_pitch", "menu_pointer_yaw", "menu_pointer_smoothing", "menu_click_debounce_ms",
			"menu_pointer_debug",
			"view_bind_force_retries",
			"eye_adjust_enabled", "left_eye_offset_x", "left_eye_offset_y",
			"right_eye_offset_x", "right_eye_offset_y",
			"hud_convergence", "freeze_time_across_eyes",
			"hud_anchor", "hud_anchor_forward", "hud_anchor_right", "hud_anchor_up",
			"hud_anchor_invert_x", "hud_anchor_invert_y", "hud_anchor_clamp",
			"hud_anchor_follow_yaw",
			"hud_anchor_scale",
			"hud_shift_skip_in_menus", "trace_eye_passes", "relax_frustum_culling",
			"relax_cull_frustum", "relax_cull_area", "relax_cull_pvs",
			"relax_cull_occlusion",
			"view_setup_ownership",
			"engine_fov", "engine_fov_margin", "engine_allow_cheats",
			"engine_portals_open_all",
			"perf_level", "engine_disable_occlusion",
			"engine_disable_vis", "engine_tune_interval_seconds",
			"log_slow_frame_ms",
			"melee_gesture", "melee_aim_forward", "melee_aim_tail", "melee_debug", "melee_min_speed", "melee_min_travel",
			"melee_min_downness", "melee_min_inward",
			"melee_max_duration", "melee_cooldown", "melee_hold_seconds",
			"arcade_reload", "arcade_reload_debug",
			"arcade_reload_dwell", "arcade_reload_max_speed",
			"arcade_reload_rearm_above", "arcade_reload_cooldown",
			"arcade_reload_hold_seconds",
			"physical_crouch", "physical_crouch_debug", "physical_crouch_trigger",
			"physical_crouch_release", "physical_crouch_compensate",
			"swim_pitch",
			"laser_dot", "laser_dot_distance", "laser_dot_size",
			"laser_dot_gap", "laser_dot_trace", "laser_dot_max_distance",
			"laser_dot_through_walls", "laser_dot_submits",
			"laser_dot_debug_tint", "laser_dot_per_weapon", "aim_debug", "aim_debug_length",
			"hand_marker", "hand_marker_size", "hand_marker_forward",
			"hand_marker_offset_forward", "hand_marker_offset_right",
			"hand_marker_offset_up",
			"two_handed", "two_handed_mode", "two_handed_debug", "two_handed_radius",
			"two_handed_release_radius", "two_handed_grip_forward",
			"two_handed_grip_right", "two_handed_grip_up", "two_handed_blend",
			"zone_melee_forward", "zone_melee_lateral", "zone_melee_up",
			"zone_melee_size_forward", "zone_melee_size_lateral",
			"zone_melee_size_up", "zone_reload_forward", "zone_reload_lateral",
			"zone_reload_up", "zone_reload_size_forward",
			"zone_reload_size_lateral", "zone_reload_size_up", "zone_boxes_visible", "zone_adjust_enabled",
			"viewmodel_anim_log", "viewmodel_anim_suppress_melee",
			"viewmodel_anim_suppress_reload", "viewmodel_anim_suppress_draw",
			"viewmodel_anim_suppress_lower",
			"viewmodel_trace_eye_passes",
			"viewmodel_diagnose",
			"viewmodel_diagnose_fire",
			"viewmodel_write_coordinate_frame",
			"weapon_entity_pin", "weapon_entity_hide",
			"viewmodel_hook_setters",
			"viewmodel_collide", "viewmodel_collide_radius", "viewmodel_collide_barrel",
			"viewmodel_collide_response", "viewmodel_collide_deflect",
			"viewmodel_collide_deflect_curve",
			"rotation_trace", "rotation_trace_frames", "compositor_poses",
			"recoil_view_lock", "recoil_view_lock_max_degrees",
			"recoil_view_lock_tail_ms",
			"cutscene_camera", "cutscene_recenter", "cutscene_frozen_latch",
			"stereo_skip_rt_projections", "stereo_rt_aspect_tolerance",
			"map_turn_to_face", "map_turn_delay_frames", "map_turn_min_degrees",
			"map_turn_on_teleport", "map_turn_teleport_units",
			"hud_anchor_transition_ms", "hud_anchor_fade_calls",
			"cutscene_look_arrow",
			"cutscene_look_arrow_fov",
			"cutscene_look_arrow_hide", "cutscene_look_arrow_fade",
			"cutscene_look_arrow_size", "cutscene_look_arrow_pulse",
			"hotkey_modifier",
			"positional_collide", "positional_collide_radius",
			"positional_collide_backoff",
			"vr_auto_resolution", "vr_resolution_scale",
			"vr_allow_oversize_window",
			"dxvk_sample_rate_shading", "dxvk_anisotropy", "vgui_cursor", "log_interfaces", "server_movement", "server_movement_slot",
			"sixdof_body", "sixdof_deadzone", "sixdof_rate", "sixdof_max_step",
			"sixdof_chase", "shot_probe", "shot_from_gun",
			"vr_max_render_height",
			"menu_cursor_d3d9", "menu_cursor_size", "menu_cursor_thickness",
			"menu_cursor_outline", "menu_cursor_r", "menu_cursor_g",
			"menu_cursor_b",
			"holster_zones", "holster_debug", "holster_left_slot",
			"holster_right_slot", "holster_hip_slot", "holster_draw_sound", "holster_grip_cycles",
		};
		count = sizeof( keys ) / sizeof( keys[0] );
		return keys;
	}

	void LoadOrCreate( const wchar_t* path )
	{
		if ( GetFileAttributesW( path ) == INVALID_FILE_ATTRIBUTES )
			WriteDefault( path );

		FILE* f = nullptr;
		if ( _wfopen_s( &f, path, L"r" ) != 0 || !f )
			return;

		char line[512];
		while ( fgets( line, sizeof( line ), f ) )
		{
			char* p = line;
			while ( *p == ' ' || *p == '\t' )
				++p;
			if ( *p == '#' || *p == ';' || *p == '\r' || *p == '\n' || *p == 0 )
				continue;

			char* eq = strchr( p, '=' );
			if ( !eq )
				continue;
			*eq = 0;

			std::string key = Trim( p );
			std::string value = Trim( eq + 1 );
			if ( !key.empty() )
				m_entries.push_back( { key, value } );
		}
		fclose( f );
		m_loaded = true;

		UpgradeMissingKeys( path );
	}

	// One parse, in one place. This loop existed three times over and the copies
	// are exactly the sort of thing that drifts.
	static void ReadEntries( const wchar_t* path,
							 std::vector<std::pair<std::string, std::string>>& out )
	{
		FILE* f = nullptr;
		if ( _wfopen_s( &f, path, L"r" ) != 0 || !f )
			return;

		char line[512];
		while ( fgets( line, sizeof( line ), f ) )
		{
			char* p = line;
			while ( *p == ' ' || *p == '\t' )
				++p;
			if ( *p == '#' || *p == ';' || *p == '\r' || *p == '\n' || *p == 0 )
				continue;
			char* eq = strchr( p, '=' );
			if ( !eq )
				continue;
			*eq = 0;
			std::string key = Trim( p );
			std::string value = Trim( eq + 1 );
			if ( !key.empty() )
				out.push_back( { key, value } );
		}
		fclose( f );
	}

	// Brings a cfg written by an older build up to date.
	//
	// ---- IT USED TO REGENERATE THE WHOLE FILE, AND THAT WAS A BUG ----------
	//
	// The old version called WriteDefault, which DISCARDS every hand-tuned value
	// and prints "Re-apply any settings you had changed". It silently reset
	// `left_handed` more than once in one session, and the owner only caught it
	// because the game came up right-handed -- a value that does not announce
	// itself would simply have stayed wrong.
	//
	// Backing the file up as .old was the mitigation, and it is a poor one:
	// UpgradeMissingKeys deletes the previous backup before every copy, so if
	// this fires twice the pre-loss copy is already gone. That is exactly how
	// the first occurrence became unrecoverable.
	//
	// So: APPEND the missing keys and change nothing else. WriteKeys already
	// rewrites line by line and preserves comments, values and ordering -- it is
	// what the in-headset tuner uses to save without destroying the file.
	void UpgradeMissingKeys( const wchar_t* path )
	{
		size_t count = 0;
		const char* const* keys = KnownKeys( count );

		std::vector<const char*> missing;
		for ( size_t i = 0; i < count; ++i )
			if ( GetString( keys[i], nullptr ) == nullptr )
				missing.push_back( keys[i] );

		if ( missing.empty() )
			return;

		// ---- NAME THEM ----------------------------------------------------
		//
		// "was missing 1 key(s)" sent two separate sessions hunting through
		// config.h for a key that both audit_config.py and a faithful
		// re-implementation of this parser said was present. A COUNT cannot be
		// checked against anything. A NAME can be grepped in one step.
		{
			std::string names;
			for ( size_t i = 0; i < missing.size(); ++i )
			{
				if ( i )
					names += ", ";
				names += missing[i];
			}
			LogWarn( "config: %d key(s) missing from sinvr.cfg: %s",
					 (int)missing.size(), names.c_str() );
		}

		wchar_t backup[MAX_PATH];
		wcscpy_s( backup, MAX_PATH, path );
		wcscat_s( backup, MAX_PATH, L".old" );
		DeleteFileW( backup );
		CopyFileW( path, backup, FALSE );

		// The defaults, generated to a scratch file purely to read values out of.
		const std::wstring scratch = std::wstring( path ) + L".defaults";
		WriteDefault( scratch.c_str() );

		std::vector<std::pair<std::string, std::string>> defaults;
		ReadEntries( scratch.c_str(), defaults );
		DeleteFileW( scratch.c_str() );

		std::vector<const char*> addKeys;
		std::vector<std::string> addVals;
		for ( const char* k : missing )
		{
			const char* v = nullptr;
			for ( const auto& d : defaults )
				if ( _stricmp( d.first.c_str(), k ) == 0 )
				{
					v = d.second.c_str();
					break;
				}

			if ( !v )
			{
				// THE "REGENERATES FOREVER" TRAP, caught at last. A key in
				// KnownKeys with no line in the generated default can never be
				// satisfied: every launch finds it missing, and before this
				// change every launch also wiped the file trying to fix it.
				LogError( "config: '%s' is in KnownKeys but has NO default line -- "
						  "it will be reported missing on every launch. Fix "
						  "config.h; run tools/audit_config.py.", k );
				continue;
			}
			addKeys.push_back( k );
			addVals.push_back( v );
		}

		if ( !addKeys.empty() )
		{
			std::vector<const char*> valPtrs;
			valPtrs.reserve( addVals.size() );
			for ( const auto& v : addVals )
				valPtrs.push_back( v.c_str() );

			if ( WriteKeys( path, addKeys.data(), valPtrs.data(), (int)addKeys.size() ) )
				Log( "config: appended %d missing key(s) with their defaults. "
					 "Every other value in the file was left alone.",
					 (int)addKeys.size() );
			else
				LogError( "config: could not write sinvr.cfg to add the missing "
						  "key(s); they will fall back to their built-in defaults" );
		}

		m_upgradedFrom = (int)missing.size();

		// Re-read so this run uses the updated file.
		m_entries.clear();
		ReadEntries( path, m_entries );
	}

	// Write values back into the cfg, updating keys in place and appending any
	// that are not there yet.
	//
	// This exists because the numpad tuner used to only PRINT the lines to
	// paste, and a tuning session done in a headset ended with values that
	// vanished when the game closed -- which is exactly what happened to the
	// per-weapon offsets on their first outing. Printing is still done, but it
	// is no longer the only record.
	//
	// Comments and every other line are preserved: the file is rewritten
	// line by line rather than regenerated, so hand-written notes survive. A
	// COMMENTED key does not count as present -- the shipped default carries
	// commented examples of the per-weapon offsets, and treating those as real
	// would mean a save silently did nothing.
	static bool WriteKeys( const wchar_t* path, const char* const* keys,
			   const char* const* values, int count )
	{
		if ( !path || !keys || !values || count <= 0 )
			return false;

		std::vector<std::string> lines;
		{
			FILE* f = nullptr;
			if ( _wfopen_s( &f, path, L"r" ) != 0 || !f )
				return false;
			char buf[512];
			while ( fgets( buf, sizeof( buf ), f ) )
			{
				std::string line( buf );
				while ( !line.empty() && ( line.back() == '\n' || line.back() == '\r' ) )
					line.pop_back();
				lines.push_back( line );
			}
			fclose( f );
		}

		std::vector<bool> written( (size_t)count, false );

		for ( auto& line : lines )
		{
			const char* p = line.c_str();
			while ( *p == ' ' || *p == '\t' )
				++p;
			if ( *p == '#' || *p == ';' || *p == 0 )
				continue;
			const char* eq = strchr( p, '=' );
			if ( !eq )
				continue;

			std::string key = Trim( std::string( p, (size_t)( eq - p ) ).c_str() );
			for ( int i = 0; i < count; ++i )
			{
				if ( written[(size_t)i] || _stricmp( key.c_str(), keys[i] ) != 0 )
					continue;
				line = std::string( keys[i] ) + " = " + values[i];
				written[(size_t)i] = true;
				break;
			}
		}

		int appended = 0;
		for ( int i = 0; i < count; ++i )
		{
			if ( written[(size_t)i] )
				continue;
			if ( appended == 0 )
			{
				lines.push_back( "" );
				lines.push_back( "# --- saved from the in-headset tuner ---" );
			}
			lines.push_back( std::string( keys[i] ) + " = " + values[i] );
			++appended;
		}

		// Write to a temp file and swap, so a crash mid-save cannot leave the
		// config truncated -- it holds everything the player has tuned.
		std::wstring tmp( path );
		tmp += L".tmp";
		{
			FILE* f = nullptr;
			if ( _wfopen_s( &f, tmp.c_str(), L"w" ) != 0 || !f )
				return false;
			for ( const auto& line : lines )
				fprintf( f, "%s\n", line.c_str() );
			fclose( f );
		}

		if ( !MoveFileExW( tmp.c_str(), path, MOVEFILE_REPLACE_EXISTING ) )
		{
			DeleteFileW( tmp.c_str() );
			return false;
		}
		return true;
	}

	int UpgradedFrom() const { return m_upgradedFrom; }

	bool Loaded() const { return m_loaded; }
	size_t Count() const { return m_entries.size(); }

	const char* GetString( const char* key, const char* fallback ) const
	{
		for ( const auto& e : m_entries )
			if ( _stricmp( e.first.c_str(), key ) == 0 )
				return e.second.c_str();
		return fallback;
	}


	int GetInt( const char* key, int fallback ) const
	{
		const char* v = GetString( key, nullptr );
		return v ? atoi( v ) : fallback;
	}

	float GetFloat( const char* key, float fallback ) const
	{
		const char* v = GetString( key, nullptr );
		return v ? (float)atof( v ) : fallback;
	}

	bool GetBool( const char* key, bool fallback ) const
	{
		const char* v = GetString( key, nullptr );
		if ( !v )
			return fallback;
		return v[0] == '1' || v[0] == 't' || v[0] == 'T' || v[0] == 'y' || v[0] == 'Y';
	}

private:
	static std::string Trim( const char* s )
	{
		std::string out( s );
		while ( !out.empty() && ( out.back() == ' ' || out.back() == '\t' ||
								  out.back() == '\r' || out.back() == '\n' ) )
			out.pop_back();
		size_t start = 0;
		while ( start < out.size() && ( out[start] == ' ' || out[start] == '\t' ) )
			++start;
		return out.substr( start );
	}

	static void WriteDefault( const wchar_t* path )
	{
		FILE* f = nullptr;
		if ( _wfopen_s( &f, path, L"w" ) != 0 || !f )
			return;

		fputs(
			"# SiN VR configuration.\n"
			"# Edit and restart the game -- this is read once at startup.\n"
			"\n"
			"# Logging verbosity: 0=error 1=warn 2=info 3=debug 4=trace\n"
			"# Use 3 or 4 when sending a log back for diagnosis. Trace logs every\n"
			"# frame and will produce a very large file, so keep runs short.\n"
			"log_level = 2\n"
			"\n"
			"# One-shot dump of every interface client.dll, engine.dll, vgui2.dll\n"
			"# and materialsystem.dll expose, by name and version.\n"
			"#\n"
			"# Every interface this mod uses was found by GUESSING a version\n"
			"# string and seeing whether the factory returned non-null. That\n"
			"# works when the guess is right and says nothing when it is wrong:\n"
			"# a null return cannot tell 'no such interface' from 'not the name\n"
			"# you thought of'. This asks the binary instead.\n"
			"#\n"
			"# Read-only -- it walks a linked list and logs names. It does not\n"
			"# instantiate anything.\n"
			"log_interfaces = 0\n"
			"\n"
			"# --- the server-side movement hook (true 6DoF groundwork) ---\n"
			"#\n"
			"# THE ONLY PART OF THIS MOD THAT TOUCHES server.dll. Everything\n"
			"# else -- head tracking, stereo, the gun, menus, holsters, aim --\n"
			"# works with this at 0, so it can be turned off without giving up\n"
			"# anything that already works.\n"
			"#\n"
			"# Why the server at all: room-scale needs the PLAYER to move, not\n"
			"# just the camera. client.dll exposes no input interface (checked\n"
			"# against all 55 of them with log_interfaces), and its GameMovement\n"
			"# is the prediction copy, which single-player does not run. The\n"
			"# authoritative one is GameMovement001 in server.dll.\n"
			"#\n"
			"# This BINDS and HOOKS ProcessMovement. On its own that is still\n"
			"# observational -- it calls the original on every path. The part\n"
			"# that writes to the move data is sixdof_body, below.\n"
			"#\n"
			"# On VAC: no exposure. Bans trigger on connecting to a VAC-secured\n"
			"# MULTIPLAYER server and SiN Episodes has none -- Arena is\n"
			"# single-player. The mod also already injects a DLL, patches four\n"
			"# modules' vtables and ships a modified DXVK.\n"
			"server_movement = 1\n"
			"\n"
			"# Which vtable slot ProcessMovement is. 1 is the expected answer --\n"
			"# MSVC puts the scalar deleting destructor at 0 -- but this game has\n"
			"# shifted slots in four other interfaces, so the dump prints the\n"
			"# vtable and this exists to correct it without a rebuild.\n"
			"server_movement_slot = 1\n"
			"\n"
			"# --- true 6DoF: walking the real play space ---\n"
			"#\n"
			"# Needs server_movement = 1 as well. This is the switch that actually\n"
			"# WRITES: each tick the player entity steps toward where your head\n"
			"# physically is, and the camera gives back exactly what the body took,\n"
			"# so the view itself does not move.\n"
			"#\n"
			"# What it buys beyond walking: the engine fires from the PLAYER, so\n"
			"# once the player is where your head is, leaning past a corner lets\n"
			"# you shoot round it. That is the same defect the aim convergence\n"
			"# hack exists to paper over.\n"
			"sixdof_body = 1\n"
			"\n"
			"# How far the head may move before the BODY follows, in units\n"
			"# (~2.5 per inch, so 12 is about 30 cm).\n"
			"#\n"
			"# This is the line between LEANING and WALKING. Inside it the head\n"
			"# moves alone, which is what lets you put your eye past a corner\n"
			"# without walking your hitbox into the open. Outside it the body\n"
			"# chases. Set it to 0 and every lean drags your collision hull\n"
			"# forward -- peeking becomes impossible.\n"
			"sixdof_deadzone = 12.0\n"
			"\n"
			"# How fast the body closes the gap, units/sec. Walking is about 40,\n"
			"# so this keeps up without lurching on a fast head turn. Lower it if\n"
			"# the body feels like it drags you; raise it if it lags behind.\n"
			"sixdof_rate = 120.0\n"
			"\n"
			"# What fraction of the remaining gap the body closes each tick.\n"
			"#\n"
			"# This is a PROPORTIONAL chase, not a constant speed, and that is what\n"
			"# stops the body hunting about the deadzone edge: the step shrinks as\n"
			"# the gap does, so it arrives asymptotically instead of running at full\n"
			"# speed into the boundary and reversing.\n"
			"#\n"
			"# Higher is snappier and closer to your real position; lower is smoother\n"
			"# but lags further behind while you walk.\n"
			"sixdof_chase = 0.15\n"
			"\n"
			"# Hard per-tick ceiling on the step, whatever the rate says. A frame\n"
			"# hitch produces a large dt, and this is what stops that becoming a\n"
			"# large jump.\n"
			"sixdof_max_step = 8.0\n"
			"\n"
			"# --- the bullet-trace probe (shots from the gun, groundwork) ---\n"
			"#\n"
			"# Hooks the SERVER's TraceRay and logs the traces that follow a trigger\n"
			"# pull. Observational: no ray is modified.\n"
			"#\n"
			"# The question it answers: can a bullet trace be told apart from the\n"
			"# thousands of movement, vision and physics traces the engine runs every\n"
			"# tick? If it can, shots can be moved to the muzzle by rewriting the ray\n"
			"# start -- and CBasePlayer's unnamed vtable never has to be touched.\n"
			"#\n"
			"# Samples 6 trigger pulls and then stops on its own, so leaving it on\n"
			"# does not fill the log. Turn it off once the answer is in.\n"
			"shot_probe = 0\n"
			"\n"
			"# --- shots from the gun, not the eye ---\n"
			"#\n"
			"# Needs server_movement = 1. Turns on the trace rewrite AND drops the aim\n"
			"# convergence, because either half alone is worse than neither: the origin\n"
			"# without the direction fires a bent angle from the muzzle, and the\n"
			"# direction without the origin fires the gun's axis from your face.\n"
			"#\n"
			"# With this on there is ONE ray -- from the muzzle, down the barrel. You\n"
			"# can shoot round a corner, because the ray starts on the far side of it.\n"
			"# aim_convergence_distance and every aim_bore_right/up value stop having\n"
			"# any effect; the aim_bore_yaw/pitch values still apply, because those\n"
			"# correct the controller's grip axis to the model's barrel axis and that\n"
			"# is still physically real.\n"
			"shot_from_gun = 1\n"
			"\n"
			"# Seconds between heartbeat lines. 0 disables them.\n"
			"log_heartbeat_seconds = 5\n"
			"\n"
			"# Render the scene once per eye. Set to 0 to go back to the mono\n"
			"# path (same image to both eyes), which is the quickest way to tell\n"
			"# a stereo problem from a submission problem.\n"
			"stereo = 1\n"
			"\n"
			"# Use the headset's real asymmetric per-eye frustum instead of the\n"
			"# game's symmetric monitor projection. This is what fixes wrong-\n"
			"# looking scale and FOV. Turn off to isolate projection maths from\n"
			"# eye-separation maths if the view looks wrong.\n"
			"stereo_projection = 1\n"
			"\n"
			"# Set the viewmodel's FOV equal to the world's. THIS IS THE MUZZLE\n"
			"# FLASH FIX, and it is not really about the FOV.\n"
			"#\n"
			"# A first-person muzzle flash is a particle system attached to the\n"
			"# viewmodel, and every one of its particles is pushed through the\n"
			"# engine's FormatViewModelAttachment on the way to the screen. That\n"
			"# takes the flash's offset FROM YOUR EYE and multiplies the sideways\n"
			"# and vertical parts of it by tan(fov/2)/tan(fovViewmodel/2) -- to\n"
			"# compensate for the viewmodel normally being drawn at a narrower FOV\n"
			"# than the world.\n"
			"#\n"
			"# On a monitor the gun is near the middle of the screen, so that\n"
			"# offset is small and the correction is small and right. In VR the\n"
			"# gun is out at your HAND, far off the eye axis, and the same rule\n"
			"# multiplies that whole distance by about 2.3 -- so the flash is\n"
			"# flung away from the gun, and it MOVES as you rotate the gun,\n"
			"# because the distance being multiplied keeps changing.\n"
			"#\n"
			"# We replace the viewmodel's projection anyway, so it is not drawn\n"
			"# squashed and there is nothing to correct. Making the two FOVs equal\n"
			"# makes the factor exactly 1.0 and the correction a no-op.\n"
			"#\n"
			"# Watch the 'viewmodel fov:' line in sinvr.log: the factor should read\n"
			"# 1.000. Set this to 0 to get the engine's original behaviour back.\n"
			"viewmodel_fov_match = 1\n"
			"\n"
			"# Let walls actually occlude the gun.\n"
			"#\n"
			"# Source renders the viewmodel into the FRONT 10% of the depth\n"
			"# buffer -- DepthRange(0, 0.1), whose own comment in the engine is\n"
			"# \"HACK HACK: Munge the depth range to prevent view model from\n"
			"# poking into walls\". The world uses the full range, so the gun can\n"
			"# never lose a depth test to anything at any distance. It never\n"
			"# clips into walls and it is never hidden by them: one behaviour,\n"
			"# both symptoms, and it reads as the gun floating on a plane of its\n"
			"# own with no depth relationship to the room.\n"
			"#\n"
			"# That hack solves a FLAT-FPS problem. There the gun sits a few\n"
			"# units from the camera and would clip the near plane; in VR it is\n"
			"# at real arm's length, well clear of it.\n"
			"#\n"
			"# Turning this on does two things together -- matching the\n"
			"# viewmodel's near/far to the world's, and widening the 0.1 slab\n"
			"# back to full range. Neither is any use alone.\n"
			"#\n"
			"# Set 0 to get the vanilla always-in-front gun back.\n"
			"viewmodel_depth = 1\n"
			"\n"
			"# Hold gameplay systems off while a menu is up.\n"
			"#\n"
			"# The main menu RUNS A BACKGROUND MAP, so the engine reports IsInGame()\n"
			"# = true there, and every gameplay gate in the mod was open on it: the\n"
			"# weapon hand steered the view instead of your head, gestures could fire,\n"
			"# and the laser traced the scenery behind the panel.\n"
			"#\n"
			"# With this on, a visible OS cursor means 'menu': aim follows the HEAD,\n"
			"# and melee, holsters, arcade reload, the two-handed grip, physical\n"
			"# crouch, movement input, the viewmodel and the world overlays are all\n"
			"# held off. Head tracking, stereo and submission stay live.\n"
			"#\n"
			"# DO NOT set this to 0 as a fix. Both positions are broken in a\n"
			"# different place, and 0 is the worse one:\n"
			"#\n"
			"#   0  the MAIN MENU locks its yaw. The background map's scripted\n"
			"#      camera re-asserts its heading every frame, the camera\n"
			"#      accumulates that as turn input, and the body yaw absorbs\n"
			"#      exactly enough to cancel the head. Pitch and roll still\n"
			"#      track, so it reads as 'rotation is broken'. You then cannot\n"
			"#      point at the menu to start a level.\n"
			"#\n"
			"#   1  correct. If gameplay ever goes dead while you are NOT in a\n"
			"#      menu, the mod now notices by itself: pushing the movement\n"
			"#      stick overrules a wrongly-reported cursor and logs\n"
			"#      'menu: LIVENESS ESCAPE'. Entering menu mode is also\n"
			"#      debounced by 6 frames so a one-frame blip cannot latch it.\n"
			"#\n"
			"# The heartbeat's 'menu:' and 'menu gating:' lines say which state\n"
			"# the mod thinks it is in; read those before changing anything.\n"
			"menu_gating = 1\n"
			"\n"
			"# How far away menus sit, as a per-eye convergence.\n"
			"#\n"
			"# A 2D panel drawn at the same screen position in BOTH eyes is not flat --\n"
			"# each eye's frustum is asymmetric the opposite way, so the panel inherits\n"
			"# the whole divergence between them and your eyes resolve it as something\n"
			"# inches from your face. That is what made the menus unreadable.\n"
			"#\n"
			"# 1.0 converges them at infinity, which is comfortable for a UI. 0 restores\n"
			"# the old behaviour. This shifts each eye by the OPPOSITE amount, so the\n"
			"# average screen position does not move and the buttons stay exactly where\n"
			"# the cursor clicks them.\n"
			"menu_convergence = 1.0\n"
			"\n"
			"# Shrink menus into a comfortable part of your view.\n"
			"#\n"
			"# A menu is drawn across the whole screen, and in VR the screen covers\n"
			"# your WHOLE field of view -- about 124 degrees here, against the ~75 a\n"
			"# monitor gives it. So the panel is not just close, it is enormous, and\n"
			"# you have to turn your head to read one corner of it. That is most of\n"
			"# what 'too close to be usable' actually is.\n"
			"#\n"
			"# 1.0 is untouched. 0.6 puts the panel in the middle 60 percent, which\n"
			"# is roughly what it would occupy on a monitor. Lower it until you can\n"
			"# take the whole menu in without moving your head.\n"
			"#\n"
			"# The pointer divides by this same number, so the buttons stay exactly\n"
			"# where you point at them. Do not try to reproduce this with a HUD key.\n"
			"menu_scale = 0.6\n"
			"\n"
			"# Draw a cross where the pointer is aiming.\n"
			"#\n"
			"# You cannot see the real cursor in VR and never could: Windows draws\n"
			"# the OS cursor onto the DESKTOP, not into the frame that is sent to\n"
			"# the headset. This is our own, drawn in the world along the pointing\n"
			"# ray at the same screen position the cursor was moved to.\n"
			"# Pin the menu where you were facing when it opened, instead of letting\n"
			"# it ride your headset.\n"
			"#\n"
			"# A 2D panel is welded to your face: turn your head and it comes with\n"
			"# you, so you can never look AT part of a big menu, only past all of\n"
			"# it. Pinned, it stays put and you can look around it -- and because\n"
			"# it is then projected per eye from a WORLD position, it converges at\n"
			"# its real distance instead of at one compromise depth.\n"
			"#\n"
			"# The pointer is handed the same placement and inverts it, so the\n"
			"# buttons stay under the cross. Set to 0 to go back to head-locked.\n"
			"# The direction is taken ONCE, from where you are facing at startup, and\n"
			"# held for the session. RECENTRE re-fixes it -- so if the panel ends up\n"
			"# somewhere awkward, face where you want it and hit recentre.\n"
			"#\n"
			"# It is deliberately not re-taken each time a menu opens: moving between\n"
			"# menu screens blinks the cursor off for a frame, so doing that re-pinned\n"
			"# the panel wherever your head happened to be and it appeared to follow\n"
			"# you after all.\n"
			"# Draw the menu as a real QUAD IN THE WORLD instead of a flat overlay.\n"
			"#\n"
			"# Every menu problem came from one fact: a 2D overlay is welded to your\n"
			"# face, has no distance, and cannot be pointed at accurately. With this\n"
			"# on, the engine is handed a projection that maps its own HUD pixels\n"
			"# onto a quad standing in front of you -- so the menu gets real\n"
			"# perspective, real per-eye depth, and stays put when you look around.\n"
			"#\n"
			"# The pointer traces against the SAME quad, so where you point and\n"
			"# where you click cannot drift apart. 0 restores the flat overlay.\n"
			"menu_world_panel = 1\n"
			"\n"
			"# How wide the quad is, in Source units. The height follows the menu's\n"
			"# own shape so nothing is stretched. Bigger is easier to read and needs\n"
			"# more head movement to take in.\n"
			"menu_panel_width = 190.0\n"
			"\n"
			"# Turn the quad to face you instead of leaving it a fixed board. Off is\n"
			"# the honest 'it is a thing in the room' behaviour; on keeps it readable\n"
			"# if it ends up further to one side than intended.\n"
			"menu_panel_billboard = 0\n"
			"\n"
			"# Align the panel with the MENU SCENE rather than with wherever you\n"
			"# happened to be facing.\n"
			"#\n"
			"# The main menu's background map has its own scripted camera, staged so\n"
			"# the character model and the logo face it. The engine re-asserts that\n"
			"# direction every frame, so it can be read and used -- which puts the\n"
			"# menu with the rest of the scene instead of off to one side of it.\n"
			"#\n"
			"# 0 pins it to your own heading at startup instead.\n"
			"menu_panel_align_scene = 1\n"
			"\n"
			"# How long the menu must have been AWAY before opening it counts as a\n"
			"# fresh open and re-places the panel in front of you. Milliseconds.\n"
			"#\n"
			"# Pinning once for the whole session left the pause menu wherever you\n"
			"# were facing when the game launched -- which in a level is usually\n"
			"# behind you. Re-pinning on every open reads as the panel following\n"
			"# your head, because moving between menu SCREENS blinks the cursor off\n"
			"# for a frame and each blink counted as a new open.\n"
			"#\n"
			"# The gap tells those apart: a blink is milliseconds, putting the menu\n"
			"# away and opening it again later is not. Raise this if submenus start\n"
			"# moving the panel; lower it if a genuine re-open leaves it behind you.\n"
			"menu_panel_repin_ms = 750\n"
			"\n"
			"# Tilt the panel. Degrees. Use with menu_anchor_yaw / _height /\n"
			"# _distance to place it exactly where you want it.\n"
			"#\n"
			"# These are manual because the right answer is NOT derivable: the menu\n"
			"# scene's character model and logo sit at a direction the engine never\n"
			"# exposes. Pinning to its view yaw produced the identical number and\n"
			"# moved nothing, which is how that was established rather than assumed.\n"
			"menu_panel_pitch = 0.0\n"
			"menu_panel_roll = 0.0\n"
			"\n"
			"# Live panel tuning on the numpad. Shares the numpad with the eye,\n"
			"# viewmodel and zone adjusters, so at most ONE of the four may be on.\n"
			"#\n"
			"#   8 / 2  further / nearer      9 / 3  up / down\n"
			"#   4 / 6  yaw left / right      7 / 1  pitch up / down\n"
			"#   .      roll                  5 5    reset all\n"
			"#   0      print cfg lines       - / +  smaller / larger step\n"
			"#\n"
			"# Numpad 0 prints the values as cfg lines, so a tuning session ends\n"
			"# with numbers to keep rather than with a feeling.\n"
			"menu_panel_adjust = 0\n"
			"\n"
			"# How the flattened menu recovers its layering.\n"
			"#\n"
			"# VGUI stacks panels using a z position, and drawing them all on one\n"
			"# quad collapses that onto a single plane -- which is what made the\n"
			"# load and options screens z-fight while the quit box, drawn alone,\n"
			"# did not. These map that z back onto depth.\n"
			"#\n"
			"# _scale converts one unit of the HUD's own z into Source units along\n"
			"# the panel normal; _step additionally separates successive draw\n"
			"# calls.\n"
			"#\n"
			"# _scale is ALSO the headroom menu_depth_fix = 5 needs: it sets the\n"
			"# window-to-window gap the per-draw nudge has to stay inside. 2.0\n"
			"# world units at a 150-unit viewing distance is about a 1% size\n"
			"# change, so this buys a lot of headroom for nothing visible.\n"
			"menu_depth_scale = 2.0\n"
			"menu_depth_step = 0.05\n"
			"\n"
			"# Draw-call measurement for the save/load shimmer. Counts every draw\n"
			"# the game makes, separates menu draws from world draws, and reads\n"
			"# back the depth state each is using. Reports on the heartbeat.\n"
			"#\n"
			"# It changes NO render state and cannot alter a frame. Set to 0 if\n"
			"# the per-draw counting ever costs measurable frame time.\n"
			"menu_draw_probe = 1\n"
			"\n"
			"# The save/load shimmer fix, applied per DRAW rather than as a global\n"
			"# state change -- so it is scoped to a single call and cannot leak the\n"
			"# way the old OverrideDepthEnable attempt did.\n"
			"#\n"
			"#   0  off\n"
			"#   1  menu draws stop WRITING depth. Minimal: panels stop fighting\n"
			"#      each other, the depth test stays. Geometry nearer than the\n"
			"#      panel could clip it.\n"
			"#   2  menu draws stop TESTING depth. Pure painter's algorithm --\n"
			"#      what VGUI expects and what flat mode does.\n"
			"#   3  as 2, but only for draws that do not WRITE depth. WRONG, and\n"
			"#      kept only so the log has a name for it: ZWRITE splits opaque\n"
			"#      from translucent, not UI from world, so this hit every decal\n"
			"#      and missed every panel -- damage AND shimmer at once.\n"
			"#   4  as 2, but only for draws whose MATERIAL is a UI material\n"
			"#      (vgui/* or Source's generated __font / __gui). Measured, not\n"
			"#      inferred: world, decal and engine draws are excluded by name.\n"
			"#      Fixes the shimmer and the geometry, but a sub-dialog then\n"
			"#      draws BEHIND its parent -- the depth test was what ordered\n"
			"#      the windows.\n"
			"#   5  same UI-only targeting, but the depth test is KEPT and each\n"
			"#      successive UI draw is nudged a little nearer instead. Breaks\n"
			"#      the tie without giving up the ordering. The default.\n"
			"menu_depth_fix = 5\n"
			"\n"
			"# Size of that nudge, in depth-buffer units (one unit = one ULP of a\n"
			"# 24-bit depth buffer). Only used by menu_depth_fix = 5.\n"
			"#\n"
			"# THIS AND menu_depth_scale ARE ONE SETTING. The nudge accumulates\n"
			"# across the draws of a single ortho call -- about 93 of them, per\n"
			"# the probe -- and that TOTAL must stay under the window-to-window\n"
			"# gap menu_depth_scale provides, or a later-drawn panel climbs past\n"
			"# the window in front of it. At 8.0 it did exactly that.\n"
			"#\n"
			"# The heartbeat prints the ramp in depth units. Keep it well under\n"
			"# the gap: RAISE this if the shimmer is still visible, LOWER it (or\n"
			"# raise menu_depth_scale) if a sub-dialog draws behind its parent.\n"
			"menu_depth_bias = 3.0\n"
			"\n"
			"menu_anchor = 1\n"
			"menu_anchor_distance = 150.0\n"
			"\n"
			"# Nudge the panel sideways and vertically from that direction.\n"
			"# Degrees, NEGATIVE is to the LEFT. SiN's menu is drawn well to the\n"
			"# right of centre, so the default pulls it back toward the middle.\n"
			"menu_anchor_yaw = -12.0\n"
			"# Source units, + is up.\n"
			"menu_anchor_height = 0.0\n"
			"\n"
			"# Draw a SECOND pointer cross through the debug overlay's screen-text\n"
			"# path. OFF, and that is a measured result rather than a preference.\n"
			"#\n"
			"# It was added to get the cursor ABOVE the menu, on the theory that\n"
			"# screen text goes through the 2D surface rather than the world pass.\n"
			"# Tested on hardware: it draws behind the menu as well, so the engine\n"
			"# puts debug overlays before VGUI whichever primitive is used. It\n"
			"# bought nothing and cost a second cross that disagrees with the first,\n"
			"# because it is drawn ONCE rather than per eye -- so it has no\n"
			"# convergence and sits in a different apparent place in each eye.\n"
			"#\n"
			"# THERE IS NO WAY TO DRAW OVER THE MENU with the interfaces bound here.\n"
			"# The feedback that does work is the menu's own highlight: point at a\n"
			"# button and VGUI lights it up, and that is what tells you the cursor\n"
			"# is on it. Kept as a switch only because it costs one line.\n"
			"menu_pointer_screen_marker = 0\n"
			"\n"
			"menu_pointer_marker = 1\n"
			"menu_pointer_marker_distance = 120.0\n"
			"# Size of the cross, in Source units at the panel's distance. 1.2 was\n"
			"# under a degree across -- thinner than a desktop mouse cursor, and easy\n"
			"# to lose against a busy level. That is the likeliest reason it read as\n"
			"# missing in a pause menu while showing fine on the sparse main menu.\n"
			"#\n"
			"# If it is still not visible at 6 or 8, size is not the problem: the\n"
			"# cross is WORLD geometry and every 2D element the game draws sits on\n"
			"# top of it, so a menu with an opaque backdrop will hide it whatever\n"
			"# this is set to.\n"
			"menu_pointer_marker_size = 3.0\n"
			"\n"
			"# --- pointing at menus ---\n"
			"#\n"
			"# SiN's menus are VGUI and are driven by the OS CURSOR, so the way to\n"
			"# click a button in VR is to move that cursor with a controller. This\n"
			"# does exactly that, and only while a menu is up.\n"
			"#\n"
			"# The menu is drawn at the same screen position in both eyes, which\n"
			"# means it is locked to your head. So where the controller points\n"
			"# RELATIVE TO YOUR HEAD maps straight onto a screen position -- no\n"
			"# trace, no world panel, no depth involved.\n"
			"#\n"
			"# Read the 'pointer:' line in sinvr.log. 'driving the cursor' with a\n"
			"# rising move count is the working case; 'NOT MOVING THE CURSOR' is\n"
			"# followed by the reason it refused.\n"
			"menu_pointer = 1\n"
			"\n"
			"# Point with the OFF hand instead of the weapon hand.\n"
			"menu_pointer_off_hand = 0\n"
			"\n"
			"# Trigger acts as a left mouse click. Set to 0 to check the pointer\n"
			"# lands on the buttons BEFORE anything starts clicking them -- a\n"
			"# mis-aimed pointer that also clicks can start a new game by itself.\n"
			"menu_pointer_click = 1\n"
			"\n"
			"# The controller-to-pointer tilt, degrees. A controller's tracked axis\n"
			"# is nowhere near where it feels like it points -- this is the same\n"
			"# correction viewmodel_angle_pitch makes, and it is set to the same\n"
			"# default so that pointing and aiming are the same gesture. If the\n"
			"# pointer sits consistently above or below where you are pointing,\n"
			"# this is the number to change.\n"
			"menu_pointer_pitch = 58.0\n"
			"menu_pointer_yaw = 0.0\n"
			"\n"
			"# 0 is raw and jittery, 0.9 is heavy and laggy. A menu button is a\n"
			"# small target and a hand is not a mouse, so some is wanted.\n"
			"menu_pointer_smoothing = 0.35\n"
			"\n"
			"# Drop a second click arriving within this many milliseconds of\n"
			"# the last one. 0 disables it.\n"
			"#\n"
			"# A double press does very different things to the two kinds of\n"
			"# control, which is why this was only ever visible on one:\n"
			"#\n"
			"#   button    fires its command twice -- invisible\n"
			"#   dropdown  ComboBox::DoClick TOGGLES, so the menu opens and\n"
			"#             shuts again and nothing appears to happen\n"
			"#\n"
			"# That was the 'I can click buttons but the anisotropic\n"
			"# filtering dropdown will not expand' report.\n"
			"#\n"
			"# 100 ms is longer than any trigger chatter and far shorter than\n"
			"# a deliberate double-click. The heartbeat reports the closest\n"
			"# gap it has actually seen, so this can be set from evidence.\n"
			"menu_click_debounce_ms = 100\n"
			"\n"
			"# Extra pointer logging.\n"
			"menu_pointer_debug = 0\n"
			"\n"
			"# Multiplier on eye separation. 1.0 is true IPD. 0 collapses to mono\n"
			"# while still running both passes. Raise it for exaggerated depth,\n"
			"# lower it if the world feels uncomfortably miniature.\n"
			"eye_separation_scale = 1.0\n"
			"\n"
			"# Live eye alignment on the numpad. Off by default because it takes\n"
			"# those keys away from the game. When on:\n"
			"#   7 / 9   select left / right eye\n"
			"#   4 6 8 2 nudge that eye left / right / up / down\n"
			"#   5       reset the selected eye\n"
			"#   - / +   halve / double the step size\n"
			"# Every change is logged with the exact cfg lines to paste below,\n"
			"# so a tuning session ends with numbers you can keep.\n"
			"eye_adjust_enabled = 0\n"
			"\n"
			"# Per-eye frustum nudge in tangent units (~0.01 is half a degree).\n"
			"left_eye_offset_x = 0.0\n"
			"left_eye_offset_y = 0.0\n"
			"right_eye_offset_x = 0.0\n"
			"right_eye_offset_y = 0.0\n"
			"\n"
			"# Per-eye shift for 2D/HUD drawing. Each eye's frustum is asymmetric in\n"
			"# the opposite direction, which is correct for 3D but makes anything\n"
			"# drawn in screen space (crosshair, HUD) appear in a different place\n"
			"# per eye. 1.0 converges it at infinity, 0 disables the correction.\n"
			"# Lower slightly if the crosshair sits too far away to fuse.\n"
			"hud_convergence = 1.0\n"
			"\n"
			"\n"
			"# --- where the HUD sits ---\n"
			"#\n"
			"# off   the game's own centred HUD, unchanged\n"
			"# body  anchored to your BODY: a fixed place relative to the way you face,\n"
			"#       so it holds still as you turn your head and you look down and across\n"
			"#       to read it\n"
			"#\n"
			"# Projected PER EYE from a world position rather than shifted to a screen\n"
			"# position, so it converges at the distance it is placed at. An earlier\n"
			"# version handed both eyes the same screen position, which is a panel at\n"
			"# infinity however near it is meant to be -- that was the 'convergence breaks\n"
			"# when it gets close' problem.\n"
			"#\n"
			"# It is still a FLAT, SCREEN-ALIGNED panel: it does not rotate to face you or\n"
			"# hang in the world. Anchoring it to the HAND was tried and needs a real\n"
			"# world-space quad instead of an ortho shift to be worth having -- see the\n"
			"# hand-mounted UI note in HANDOVER.md.\n"
			"hud_anchor = body\n"
			"\n"
			"# Where the panel sits relative to your body, in units. Forward is how far in\n"
			"# front -- and therefore the depth it converges at. Right is negative for the\n"
			"# left side. Up is negative for below eye level.\n"
			"hud_anchor_forward = 75.0\n"
			"hud_anchor_right = 25.0\n"
			"hud_anchor_up = -10.0\n"
			"\n"
			"# Which yaw the panel follows. Pitch is ignored either way -- that is what\n"
			"# keeps it below your view until you look down; if it pitched with your head\n"
			"# it would sink as you looked and never come into sight.\n"
			"#\n"
			"# 1  HEAD yaw: stays in front of you wherever you turn, always findable.\n"
			"# 0  BODY yaw: pinned to the world, so with stick turning it can end up\n"
			"#    behind you and never be found.\n"
			"hud_anchor_follow_yaw = 1\n"
			"\n"
			"# Flip an axis if it moves the wrong way, without a rebuild.\n"
			"hud_anchor_invert_x = 0\n"
			"hud_anchor_invert_y = 0\n"
			"\n"
			"# Keep the panel on screen rather than letting it slide out of view. Clamped\n"
			"# to a margin rather than 0..1, since the HUD has width.\n"
			"hud_anchor_clamp = 1\n"
			"\n"
			"# Size of the panel on screen. Below 1 shrinks it; 0.6 is a little over half.\n"
			"#\n"
			"# This scales the HUD rather than pushing it further away, which is the\n"
			"# distinction that matters here: the panel is an ortho projection, so moving\n"
			"# it out changes where it converges but NOT how big it looks. Only this does.\n"
			"#\n"
			"# Caveat: it scales everything drawn through Ortho while the HUD is anchored,\n"
			"# so anything meant to cover the whole screen -- a damage flash, a fade --\n"
			"# shrinks with it. Menus are already excluded via hud_shift_skip_in_menus.\n"
			"hud_anchor_scale = 1.0\n"
			"\n"
			"# Leave interactive UI (pause menu, save, options) unshifted. The\n"
			"# convergence shift moves where those windows draw, but not the mouse\n"
			"# hit-testing, so their buttons stop matching the cursor. Detected by\n"
			"# the OS cursor being visible, which Source only does for menus.\n"
			"hud_shift_skip_in_menus = 1\n"
			"\n"
			"# Log the view/projection sequence for this many eye passes at start.\n"
			"# Shows how many distinct views Source sets up per pass (main scene,\n"
			"# 3D skybox, viewmodel), which is needed to work out why some objects\n"
			"# are not consistent between eyes. 0 disables.\n"
			"trace_eye_passes = 2\n"
			"\n"
			"# Master switch for relaxing the engine's visibility tests during eye\n"
			"# passes. Source culls against the game's symmetric FOV on a 4:3 view,\n"
			"# but each eye's real frustum is wider and asymmetric, so visible\n"
			"# geometry gets thrown away -- worst toward the outer edge of each eye.\n"
			"# Note this only reaches ENTITIES. World brush surfaces are culled\n"
			"# inside engine.dll where no hook can reach them, so a missing ceiling\n"
			"# is fixed by engine_fov below, not by anything here.\n"
			"relax_frustum_culling = 1\n"
			"\n"
			"# The four tests individually, for bisecting. All only take effect when\n"
			"# relax_frustum_culling is 1.\n"
			"#   frustum   CullBox / IsBoxVisible -- genuinely too narrow for a VR\n"
			"#             eye, so this one really is needed\n"
			"#   area      DoesBoxTouchAreaFrustum -- direction-dependent, also\n"
			"#             needed, but forcing it draws entities from rooms you\n"
			"#             cannot see\n"
			"#   pvs       IsBoxInViewCluster -- computed from the view ORIGIN, so\n"
			"#             it cannot explain anything that changes as you look\n"
			"#             around. Turn off first if entities appear through walls.\n"
			"#   occlusion IsOccluded -- func_occluder volumes\n"
			"#\n"
			"# NOTE these were all needed when the engine's culling frustum was too\n"
			"# small. It no longer is -- the mod writes it directly and the heartbeat's\n"
			"# 'stereo frustum:' line verifies every few seconds that it contains\n"
			"# everything we render. So these are now redundant insurance that costs\n"
			"# frame time. If you are chasing performance, turn them off one at a time\n"
			"# and watch 'stereo frame cost:'. If world geometry goes missing again,\n"
			"# 'stereo frustum:' will say SHORT and that is a different problem.\n"
			"relax_cull_frustum = 1\n"
			"relax_cull_area = 1\n"
			"relax_cull_pvs = 0\n"
			"relax_cull_occlusion = 1\n"
			"\n"
			"# Warn in the heartbeat when one frame's eye passes take longer than this,\n"
			"# in milliseconds. The display driver resets the GPU at around 2000 ms,\n"
			"# which is the VK_ERROR_DEVICE_LOST / LiveKernelEvent 0x141 crash -- and\n"
			"# from inside the process that looks like a freeze with no error at all.\n"
			"# This is set well below the cliff so the ramp is visible first.\n"
			"log_slow_frame_ms = 500\n"
			"\n"
			"# Drive the client's own view (CViewSetup) rather than patching the\n"
			"# matrices it produces. This is the good path:\n"
			"#   - the culling FOV is written straight into the view every frame,\n"
			"#     so no cvars, no sv_cheats, and a level load cannot revert it\n"
			"#   - the eye offset is one assignment to the view origin, instead of\n"
			"#     catching every view matrix on its way to the material system and\n"
			"#     guessing whether it is one of ours coming back. That guess needs a\n"
			"#     ring of the last 8 results and this engine sets up more views than\n"
			"#     that per pass, so some were being offset twice -- which is wrong\n"
			"#     stereo depth on whatever they drew.\n"
			"# Falls back to the old matrix path automatically if the view cannot be\n"
			"# found or fails its sanity check. Set to 0 to force the old path.\n"
			"view_setup_ownership = 1\n"
			"\n"
			"# Only used when view_setup_ownership is 0 or fell back.\n"
			"# Widen the game's own FOV. This does NOT change the VR image -- the\n"
			"# projection is replaced per eye regardless -- but the engine builds\n"
			"# its world-culling frustum from this value by angle, never from our\n"
			"# matrix. At the stock 75 it discards roughly the outer 23% of image\n"
			"# height and 30% of width: ceilings, upper walls and edge geometry the\n"
			"# headset can plainly see. This is THE setting for missing world\n"
			"# geometry.\n"
			"#\n"
			"#   0   work it out from the headset's own frustum (recommended -- the\n"
			"#       old hand-picked 110 was measurably too small, the G2 at 4:3\n"
			"#       needs about 117 before any margin)\n"
			"#   >0  use exactly this value\n"
			"#   <0  leave the game's FOV alone\n"
			"#\n"
			"# The heartbeat's 'stereo frustum:' line reports what the engine is\n"
			"# actually culling with, so you can see whether it took.\n"
			"engine_fov = 0\n"
			"\n"
			"# Safety factor on the computed FOV. The engine's frustum apex sits at\n"
			"# the head centre while each eye renders from ~1.25 units to the side,\n"
			"# so an exact fit still clips near geometry.\n"
			"engine_fov_margin = 1.05\n"
			"\n"
			"# Enable sv_cheats. default_fov and the r_* visibility cvars below are\n"
			"# cheat-flagged in Source and are silently rejected without it. Set to 0\n"
			"# if you would rather not, at the cost of only fov_desired applying --\n"
			"# which the engine clamps.\n"
			"engine_allow_cheats = 1\n"
			"\n"
			"# r_portalsopenall 1. Area portals are evaluated in the ENGINE's screen\n"
			"# space, so a doorway falling outside a too-narrow frustum would close the\n"
			"# whole area behind it. That was the theory; the actual cause of the\n"
			"# missing geometry was the culling frustum itself, which is now set\n"
			"# directly and verified every heartbeat.\n"
			"#\n"
			"# So this now defaults OFF. Forcing every portal open makes the engine\n"
			"# draw the entire map, twice per frame, and in a corridor game that is\n"
			"# most of the frame cost for no visual benefit. Turn it back on only if\n"
			"# whole rooms vanish while 'stereo frustum:' still reports OK.\n"
			"engine_portals_open_all = 1\n"
			"\n"
			"# ==== EXPERIMENTAL performance ====\n"
			"#\n"
			"# One dial that turns off several pieces of caution at once.\n"
			"#\n"
			"# Every setting behind this already had its own key, and every\n"
			"# one of them ships in the safe-and-slow position -- each for a\n"
			"# good reason, found separately, and never measured together.\n"
			"# This is the measurement, offered as a setting.\n"
			"#\n"
			"# The levels are ordered by how likely you are to SEE something\n"
			"# go wrong, not by how much they save, so you meet the invisible\n"
			"# ones first:\n"
			"#\n"
			"#   0  off. Everything as shipped.\n"
			"#\n"
			"#   1  Occlusion culling back on, and the engine's culling FOV\n"
			"#      trimmed to exactly what the headset actually shows.\n"
			"#      Measured 5.4% of pure slack, and frustum cost rises with\n"
			"#      the SQUARE of the angle. No visual change expected.\n"
			"#\n"
			"#   2  Also stops second-guessing the engine's culling. On one\n"
			"#      measured frame the mod drew 32238 of 71190 objects the\n"
			"#      engine had already decided were invisible -- 45%. That\n"
			"#      override predates a later fix that solved the same\n"
			"#      problem properly, so it may be pure cost. If it was not,\n"
			"#      you will see scenery pop in at the edge of vision.\n"
			"#\n"
			"#   3  Also lets doorways block what is behind them again. This\n"
			"#      is the big one indoors, and SiN is almost all indoors.\n"
			"#      The catch is known and is why it is not the default:\n"
			"#      standing IN a doorway, one eye can briefly lose the room\n"
			"#      beyond. Some players will take that for the frame rate.\n"
			"#\n"
			"# HOW TO USE IT. Play the same spot at level 0, then again at\n"
			"# the level you want, and compare the `stereo frame cost` line\n"
			"# in sinvr.log. If the number did not move, put it back -- an\n"
			"# artifact you cannot measure a saving for is a bad trade.\n"
			"#\n"
			"# This OVERRIDES the individual culling keys below. Set it to 0\n"
			"# if you want to tune them by hand.\n"
			"perf_level = 0\n"
			"\n"
			"# r_occlusion 0. func_occluder volumes were placed for a 75-degree view.\n"
			"engine_disable_occlusion = 1\n"
			"\n"
			"# r_novis 1. Disables PVS entirely -- draws the whole map. Expensive and\n"
			"# almost certainly not the cause, since the PVS is computed from the\n"
			"# view origin and so cannot change as you turn your head. Here to rule\n"
			"# it out in one run, not to leave on.\n"
			"engine_disable_vis = 0\n"
			"\n"
			"# How often to re-assert all of the above, in seconds. It has to repeat:\n"
			"# a level load resets the FOV, and the first frames of a session run on\n"
			"# the menu's background map where the game already counts as 'in game'.\n"
			"# Applying once there is exactly how the FOV silently reverted for a\n"
			"# whole play session. 0 applies only on entering a map.\n"
			"engine_tune_interval_seconds = 5\n"
			"\n"
			"# Render both eyes at one frozen engine time. In theory this stops the\n"
			"# two eyes sampling different instants, which is part of the jitter.\n"
			"# In practice it destroyed performance -- 7 frames per 5 seconds\n"
			"# instead of 450, then a hang -- so something in Source's render path\n"
			"# waits on time advancing. Leave at 0 unless you are investigating it.\n"
			"freeze_time_across_eyes = 0\n"
			"\n"
			"# Send frames to the headset. Requires the SiN VR build of d3d9.dll\n"
			"# (the one exporting Direct3DCreateVR9) next to the exe, plus a copy\n"
			"# of openvr_api.dll named openvr_api_dxvk.dll -- DXVK looks for that\n"
			"# name when enabling the Vulkan extensions the compositor needs.\n"
			"# Set to 0 to run head tracking only, which is the way to tell a\n"
			"# submission problem apart from a tracking problem.\n"
			"submit_frames = 1\n"
			"\n"
			"# TEST ONLY. Pretend the view is not ready for this many bind\n"
			"# attempts, then let it bind normally.\n"
			"#\n"
			"# View ownership binds lazily because the real bind can land before\n"
			"# the first map, when the view is legitimately 0x0. Treating that as\n"
			"# a layout mismatch once cost a whole session on the matrix path --\n"
			"# broken head and controller tracking, and an error message that\n"
			"# confidently blamed the struct layout.\n"
			"#\n"
			"# The recovery is a race, so it cannot be tested by waiting. Set this\n"
			"# to something like 120 and watch the log:\n"
			"#\n"
			"#   view: FORCING 120 not-ready bind attempts (test only)\n"
			"#   view: ownership active after 121 attempt(s)\n"
			"#\n"
			"# Tracking should be wrong until that second line and correct after\n"
			"# it, in the same session. That is the whole thing being tested: not\n"
			"# just that the retry fires, but that switching to ownership\n"
			"# mid-session actually recovers.\n"
			"#\n"
			"# Leave at 0.\n"
			"view_bind_force_retries = 0\n"
			"\n"
			"# Master switch for the VR camera. If head tracking is making the game\n"
			"# unplayable, set this to 0 to get a normal flatscreen game with the\n"
			"# mod still loaded and logging.\n"
			"vr_enabled = 1\n"
			"\n"
			"# Source units per real-world metre. 39.37 is geometrically correct\n"
			"# (1 unit = 1 inch). Larger makes the world feel smaller. Only affects\n"
			"# positional tracking, so it does nothing until 6DoF lands.\n"
			"world_scale = 39.37\n"
			"\n"
			"# Apply head roll to the view. Some people find roll nauseating.\n"
			"apply_roll = 1\n"
			"\n"
			"# Clamp for view pitch, in degrees. Source misbehaves near +-90.\n"
			"pitch_limit = 89\n"
			"\n"
			"\n"
			"# --- 6DoF positional tracking ---\n"
			"#\n"
			"# Moves the view with your head: leaning, crouching, and stepping\n"
			"# inside the play space. Measured from where you were standing at the\n"
			"# last recentre (F9), not from the play space origin.\n"
			"#\n"
			"# NOT collided. Leaning into a wall puts the camera inside it --\n"
			"# positional_max_offset is what bounds that until eye tracing lands.\n"
			"positional_tracking = 1\n"
			"\n"
			"# Multiplier on real head movement. 1.0 is life-size; above 1 makes a\n"
			"# small play space reach further at the cost of feeling wrong.\n"
			"positional_scale = 1.0\n"
			"\n"
			"# Hard ceiling on head displacement, in Source units (~1 inch each,\n"
			"# so 80 is about 2 m). Bounds a tracking glitch that would otherwise\n"
			"# throw the camera across the level, and bounds how far you can lean\n"
			"# through geometry.\n"
			"positional_max_offset = 80.0\n"
			"\n"
			"# --- motion controllers ---\n"
			"#\n"
			"# Reads the OpenVR action manifest in actions\\ beside the exe and\n"
			"# drives the game with console commands. Rebind anything you like in\n"
			"# SteamVR's own controller binding UI -- the manifest is what puts\n"
			"# 'SiN VR' in that list.\n"
			"#\n"
			"# Defaults: left stick moves, right stick turns, triggers fire,\n"
			"# A jumps, B uses, X reloads, Y is the flashlight, left grip crouches,\n"
			"# right grip cycles weapons, right stick click recentres.\n"
			"controller_input = 1\n"
			"\n"
			"# Stick travel treated as centred. Source movement is binary here\n"
			"# (+forward, not an axis), so a generous deadzone costs nothing.\n"
			"controller_move_deadzone = 0.35\n"
			"controller_turn_deadzone = 0.35\n"
			"\n"
			"# Snap turning by default -- smooth stick turning makes a lot of\n"
			"# people ill, especially in a fast shooter. Set to 0 for smooth.\n"
			"controller_snap_turn = 1\n"
			"controller_snap_degrees = 30.0\n"
			"\n"
			"# --- the turn stick's VERTICAL axis ---\n"
			"#\n"
			"# Turning only ever used the stick's horizontal axis, so its vertical\n"
			"# one was free -- and it is worth more than any leftover button,\n"
			"# because every controller has it. Vive wands in particular had no\n"
			"# free physical input at all.\n"
			"#\n"
			"# It follows left_handed AND swap_thumbsticks for nothing: the stick\n"
			"# that feeds Turn is chosen before this sees it, so 'down on the stick\n"
			"# you turn with' stays true in every configuration.\n"
			"#\n"
			"# turn_stick_crouch frees the GRIP, which was the only thing crouch\n"
			"# was on.\n"
			"turn_stick_crouch = 1\n"
			"\n"
			"# Push UP to throw a grenade. Off by default -- on stick controllers\n"
			"# there is a real button free for it, and a thumb pushed forward while\n"
			"# turning is a normal thing to do. Turn it on for Vive wands, where\n"
			"# every physical input is spoken for and this is the only way to reach\n"
			"# a grenade at all.\n"
			"turn_stick_grenade = 0\n"
			"\n"
			"# Throw grenades with the OFF hand's grip.\n"
			"#\n"
			"# On Touch and WMR every input is spoken for -- both triggers, A/B,\n"
			"# X/Y, both sticks and both grips -- so grenades used to have no\n"
			"# button at all and turn_stick_grenade was the only way to get one.\n"
			"#\n"
			"# The off hand's grip is the exception. Both grips carry NextWeapon\n"
			"# in the bindings, but the mod reads it per device: the WEAPON hand's\n"
			"# grip draws from a holster, and the other one does nothing unless\n"
			"# two_handed_mode is 'toggle' or 'hold'. In the default 'auto' the\n"
			"# foregrip needs no button, so the grip is free and grenades take it.\n"
			"#\n"
			"# THIS TURNS ITSELF OFF when two_handed_mode is 'toggle' or 'hold'.\n"
			"# The foregrip has first claim -- throwing a grenade every time you\n"
			"# steadied your rifle would be worse than having no grenade button.\n"
			"# In that case use turn_stick_grenade = 1 instead. The log says which\n"
			"# of the two is live on every launch.\n"
			"off_hand_grenade = 1\n"
			"\n"
			"# How far the stick must go, 0..1. The push must ALSO be more vertical\n"
			"# than horizontal, which is what stops a hard diagonal turn from also\n"
			"# ducking you -- so this can be generous without turning becoming a\n"
			"# crouch. Raise it if you crouch while steering.\n"
			"turn_stick_threshold = 0.65\n"
			"\n"
			"# The menu needs TWO clicks of the stick, not one.\n"
			"#\n"
			"# A thumbstick click is easy to catch by accident and opening the menu\n"
			"# mid-firefight is worse than any other misfire in the game. Which\n"
			"# stick it is follows left_handed and swap_thumbsticks: it is always\n"
			"# the one that does NOT recentre.\n"
			"#\n"
			"# Done in the mod rather than as a SteamVR 'double' binding, because\n"
			"# stacking a second button-mode source onto a path the joystick source\n"
			"# already claims did not fire, and from outside the runtime there is no\n"
			"# way to see why. Here it works the same on every controller and the\n"
			"# heartbeat can report it.\n"
			"#\n"
			"# Set to 0 for a single click.\n"
			"menu_double_click = 1\n"
			"\n"
			"# How long the second click has, in milliseconds.\n"
			"menu_double_click_ms = 400\n"
			"\n"
			"# Log both edges of the Use button with the state that decides where\n"
			"# the +use trace actually lands: how far the engine's aim sat from\n"
			"# your head, and how far the camera sat from the origin the trace\n"
			"# starts from. You can be looking straight at something and still be\n"
			"# aiming, and tracing, from somewhere else.\n"
			"#\n"
			"# For chasing a use that does not take -- the intro car sequence soft\n"
			"# locked once this way, and the retry worked, so what is wanted is the\n"
			"# state on the attempt that FAILS. Logs per press, not per frame.\n"
			"use_trace = 0\n"
			"\n"
			"# Resolve Use along your GAZE instead of along the gun.\n"
			"#\n"
			"# With aim_source = controller the engine's aim pitch is the\n"
			"# controller's pose PLUS viewmodel_angle_pitch, so the +use trace runs\n"
			"# 9-24 degrees below where you are looking. That is correct for\n"
			"# shooting -- the muzzle has to match the tilted gun -- and wrong for\n"
			"# reaching a door handle. Measured on the intro car, which soft locked\n"
			"# because the door opened and the seat never took the use.\n"
			"#\n"
			"# Same mechanism melee already uses (melee_aim_forward).\n"
			"#\n"
			"# ADDED 2026-09-01 and NOT yet tested with a weapon in hand -- if\n"
			"# shooting or melee starts behaving oddly, set this to 0 first.\n"
			"use_aim_head = 1\n"
			"\n"
			"# How long gaze aim outlives the Use button, in seconds. The window is\n"
			"# refreshed every frame the button is held, so this is only the tail\n"
			"# after you let go.\n"
			"use_aim_tail = 0.20\n"
			"\n"
			"# Dump IMaterialSystem's whole vtable once at startup: index, address,\n"
			"# offset from the module base, and the first twelve bytes of each\n"
			"# function.\n"
			"#\n"
			"# For finding a slot the mod does not have an index for yet. The slot\n"
			"# table in source_interfaces.h cannot be interpolated -- Ritual inserted\n"
			"# methods in more than one place, so the shift from the SDK is +4 in one\n"
			"# block and +3 in another. A new index has to be read out of the running\n"
			"# game and identified by its disassembly.\n"
			"#\n"
			"# ~140 log lines, once. Archaeology, not telemetry -- leave it off.\n"
			"matsys_vtable_dump = 0\n"
			"\n"
			"# Record WHICH MATERIAL is drawn in which ortho window, and report the\n"
			"# per-heartbeat DELTA for each pair.\n"
			"#\n"
			"# This is the discriminator the fade hunt has been missing. The HUD and\n"
			"# a full-screen fade are byte-identical in every argument Ortho\n"
			"# receives -- extents and z alike -- so nothing at that hook can tell\n"
			"# them apart. The material can.\n"
			"#\n"
			"# Read the DELTA column, not the total: a fade material sits at 0\n"
			"# through normal play and spikes for the one heartbeat containing the\n"
			"# flash. Take damage, or trigger a heal, while this is on.\n"
			"#\n"
			"# Observational only -- it records a name and calls through.\n"
			"hud_material_trace = 0\n"
			"\n"
			"controller_smooth_turn_speed = 120.0\n"
			"\n"
			"# Where 'forward' is when you push the stick forward.\n"
			"#\n"
			"#   hmd        - you walk where you look. Source's own behaviour.\n"
			"#   controller - you walk where the off-hand controller points, so\n"
			"#                walking and looking are independent.\n"
			"#\n"
			"# Movement is still quantised to the engine's four directions, so\n"
			"# 'controller' picks the nearest of those four -- not free strafing.\n"
			"movement_direction = hmd\n"
			"\n"
			"# Swap which physical controller is the weapon hand and which is the\n"
			"# movement hand. The viewmodel is still drawn right-handed by the\n"
			"# game -- this decides which controller AIMS, not which arm is drawn.\n"
			"# Mirroring the viewmodel itself needs controller tracking first.\n"
			"left_handed = 0\n"
			"\n"
			"# Swap the two thumbsticks, independently of left_handed.\n"
			"#\n"
			"# left_handed mirrors EVERY pair at once -- triggers, face buttons,\n"
			"# grips and sticks -- so a left-handed player who wants the mirrored\n"
			"# triggers but their movement thumb back where it started has no way\n"
			"# to say so. This is that second axis, and it COMBINES with\n"
			"# left_handed rather than replacing it:\n"
			"#\n"
			"#   left_handed = 0, swap = 0   move LEFT stick,  turn RIGHT\n"
			"#   left_handed = 0, swap = 1   move RIGHT stick, turn LEFT\n"
			"#   left_handed = 1, swap = 0   move RIGHT stick, turn LEFT\n"
			"#   left_handed = 1, swap = 1   move LEFT stick,  turn RIGHT, with\n"
			"#                               triggers and buttons still mirrored\n"
			"#\n"
			"# Jump and reload travel with the sticks too. Jump belongs under the\n"
			"# thumb that is NOT steering -- the gamepad convention, and what lets\n"
			"# you jump while running; following left_handed alone put movement and\n"
			"# jump on the same thumb. Use and flashlight stay with left_handed.\n"
			"#\n"
			"# The stick CLICK travels with its stick. Which action that is gets\n"
			"# read out of your live SteamVR bindings rather than assumed, so it\n"
			"# is menu/recentre on stick controllers and crouch/jump on Vive\n"
			"# wands -- and it stays right if you rebind. The log line 'sticks:'\n"
			"# reports what it resolved to.\n"
			"swap_thumbsticks = 0\n"
			"\n"
			"# What the game aims, shoots and walks along.\n"
			"#\n"
			"#   hmd        - you shoot where you look. Unchanged behaviour.\n"
			"#   controller - the weapon hand aims and the head only decides what\n"
			"#                you see, so you can cover a doorway while looking\n"
			"#                somewhere else.\n"
			"#\n"
			"# The crosshair follows the gun, so it will no longer sit in the\n"
			"# middle of your view -- that is correct, not a bug. The viewmodel is\n"
			"# still drawn from the view origin until it is attached to the\n"
			"# controller, so the gun will not visually line up yet.\n"
			"#\n"
			"# Falls back to head aim whenever the weapon hand is not tracked.\n"
			"aim_source = controller\n"
			"\n"
			"# Source fires from the player's EYE, not from the weapon model, so\n"
			"# aiming straight down the barrel sends the shot along a line PARALLEL\n"
			"# to it, displaced by however far the gun sits from your eye. It lands\n"
			"# consistently off to one side, and the side flips with left_handed.\n"
			"#\n"
			"# This is the distance, in Source units (~1 inch each), at which the\n"
			"# eye's line and the barrel's line meet -- the same idea as iron\n"
			"# sights. Exact at that range and much closer either side of it than\n"
			"# the constant offset it replaces. 250 is roughly 6 m.\n"
			"# Set 0 to aim straight down the barrel instead.\n"
			"#\n"
			"# With aim_convergence_trace on, this is only the FALLBACK for when\n"
			"# the barrel is pointing at open sky and the trace hits nothing.\n"
			"aim_convergence_distance = 250.0\n"
			"\n"
			"# MEASURE that distance instead of guessing it.\n"
			"#\n"
			"# A fixed convergence is exact at exactly one range. This traces\n"
			"# down the barrel to whatever it is really pointing at and converges\n"
			"# there, so the engine's eye-to-target line passes through the same\n"
			"# point the gun does at EVERY range -- not a better approximation,\n"
			"# an exact one.\n"
			"#\n"
			"# Measured BEFORE the collision deflection, so a gun tilted out of a\n"
			"# wall still shoots where you pointed it rather than where the\n"
			"# tilted model faces.\n"
			"#\n"
			"# Still wrong in one narrow case: when the line from your EYE to the\n"
			"# target is blocked by something the line from the MUZZLE is not --\n"
			"# the gun poking round a corner with your head still behind it.\n"
			"aim_convergence_trace = 1\n"
			"\n"
			"# How far down the barrel to look for a target, in Source units.\n"
			"# Beyond this the fixed distance above is used instead.\n"
			"aim_convergence_max = 4096.0\n"
			"\n"
			"# --- zeroing the sights ---\n"
			"#\n"
			"# Where the BARREL'S AXIS sits relative to the controller, in Source\n"
			"# units. NEGATIVE right is to the LEFT. This is what makes the\n"
			"# model's iron sights agree with where the bullets actually land.\n"
			"#\n"
			"# A DISTANCE, not an angle, on purpose. The resulting correction is\n"
			"# atan(offset / range), so it is right at every distance by itself.\n"
			"# An angular trim would match at the range it was tuned at and be\n"
			"# wrong everywhere else -- sight alignment is a parallax problem and\n"
			"# takes a parallax fix.\n"
			"#\n"
			"# TO TUNE: aim down the sights at a wall and fire. The crosshair is\n"
			"# where the bullet goes, so if it lands RIGHT of the sights make\n"
			"# this MORE negative, and if it lands LEFT make it less. One unit is\n"
			"# about an inch, and at 150 units of range one unit is roughly 0.4\n"
			"# of a degree.\n"
			"#\n"
			"# One value for three guns whose sights sit differently. If it will\n"
			"# not zero for all of them, the per-weapon offsets in\n"
			"# ModelOffsetsFor are the pattern to follow.\n"
			"aim_bore_right = -3.0\n"
			"aim_bore_up = 0.0\n"
			"#\n"
			"# The muzzle's position ALONG the barrel. Only meaningful with\n"
			"# shot_from_gun, where these three stop being a parallax fudge and\n"
			"# become where the shot physically leaves the gun -- which is why a\n"
			"# forward value exists at all: sliding a convergence target along\n"
			"# the aim axis changed nothing, but moving the muzzle along it\n"
			"# decides how far past a corner you can shoot from.\n"
			"aim_bore_fwd = 0.0\n"
			"\n"
			"# Zero the sights IN THE HEADSET, per weapon.\n"
			"#\n"
			"#   '   cycle which axis , and . drive\n"
			"#   ,   decrease it       .   increase it\n"
			"#   /   SAVE the gun in hand -- all four axes at once\n"
			"#\n"
			"# Sighting is a look-and-adjust job -- the only instrument that\n"
			"# works is your own eye down the sights -- and the three guns carry\n"
			"# their sights differently, so each gets its own value. Saving\n"
			"# writes aim_bore_right_<model>, e.g. aim_bore_right_v_magnum.\n"
			"#\n"
			"# Those per-weapon keys are OPTIONAL: absent by default, and NOT\n"
			"# in KnownKeys. Do not add them there -- every launch would then\n"
			"# see missing keys. A gun with no saved value uses the globals\n"
			"# above.\n"
			"#\n"
			"# Set to 0 once tuning is done: these are bare keys, so they stay\n"
			"# live during normal play while this is on.\n"
			"aim_bore_adjust = 1\n"
			"\n"
			"# Units moved per key press. Halve it for a fine pass once the\n"
			"# sights are close.\n"
			"aim_bore_step = 0.5\n"
			"\n"
			"# --- the ANGULAR half of the zero ---\n"
			"#\n"
			"# aim_bore_right above is a DISTANCE, which is right for a real bore\n"
			"# offset: the correction it produces is atan(offset/range), so it\n"
			"# holds at every distance by itself.\n"
			"#\n"
			"# That is not the whole story here. Measured in the headset: the\n"
			"# crosshair drifts in and out of alignment as the target gets nearer\n"
			"# or further, which a lateral offset structurally cannot cause. And\n"
			"# the values needed were around 25 units -- 65 cm of bore offset on a\n"
			"# gun held in one hand, which is not a real distance.\n"
			"#\n"
			"# Both are explained by an ANGULAR error: each viewmodel is authored\n"
			"# with its barrel and sights at a slightly different angle to the\n"
			"# model's forward, so a parallax term was cancelling an angle. That\n"
			"# matches at exactly one range and drifts either side of it.\n"
			"#\n"
			"# TO TUNE: aim at something FAR away and set the yaw until the\n"
			"# crosshair sits on the sights -- distance barely matters up there,\n"
			"# so it isolates the angle. THEN aim at something close and use\n"
			"# aim_bore_right for whatever is left, which is the genuine offset.\n"
			"# Same order as zeroing a real rifle: sights first, then parallax.\n"
			"#\n"
			"# Degrees. Yaw + is LEFT, pitch + is UP.\n"
			"aim_bore_yaw = 0.0\n"
			"aim_bore_pitch = 0.0\n"
			"\n"
			"# Degrees per key press for the two angular axes.\n"
			"aim_bore_angle_step = 0.25\n"
			"\n"
			"# --- which point the aim is measured FROM ---\n"
			"#\n"
			"# The aim path outputs ANGLES, so its origin is implicit: whatever\n"
			"# the hand vector is measured relative to. Get that wrong and the\n"
			"# engine is handed a direction computed from one point and fires it\n"
			"# from another, so the shot misses by however far apart they are.\n"
			"#\n"
			"# 0 (default) measures from the RECENTRE REFERENCE, which in world\n"
			"# space is the engine's eye -- the 6DoF offset is added on top of\n"
			"# that for rendering, and the engine fires from the un-offset point\n"
			"# because positional tracking deliberately does not move the player.\n"
			"#\n"
			"# 1 measures from your PHYSICAL HEAD, which is the old behaviour.\n"
			"# It is a different point whenever you lean or turn, so the shot\n"
			"# drifts by exactly your head movement.\n"
			"#\n"
			"# Kept switchable rather than just corrected, because this has been\n"
			"# reasoned about wrongly twice. With aim_debug = 1 you can SEE it:\n"
			"# lean, and watch whether the red line stays on the white cross.\n"
			"aim_origin_head = 0\n"
			"\n"
			"# SiN kicks the view when you fire, and the engine adds that kick to\n"
			"# the SHOT as well as the picture -- player.cpp:5711 fires along\n"
			"# EyeAngles() + m_vecPunchAngle. On a monitor you see the gun climb\n"
			"# and pull down without thinking. Here the view comes from your head,\n"
			"# so the kick never appears: the shots just drift upward with no\n"
			"# feedback. weapon_magnum.txt ramps pitch 1.0 -> 1.5 the longer fire\n"
			"# is held, and yaw/roll are random either side, so it is a systematic\n"
			"# climb rather than scatter.\n"
			"#\n"
			"#   0    recoil untouched, as the designers intended\n"
			"#   0.5  halved -- keeps the feel without the blind drift\n"
			"#   1    removed entirely\n"
			"#\n"
			"# Off by default: this is a balance change, not a bug fix.\n"
			"recoil_compensation = 0.0\n"
			"\n"
			"# Source draws the crosshair at SCREEN CENTRE -- it is a HUD element,\n"
			"# not a world-space aim marker. Once the weapon aims independently of\n"
			"# the head it therefore sits in the middle of your view, which is\n"
			"# precisely where you are NOT shooting. Hidden automatically while\n"
			"# aim_source = controller. Set to 0 to keep it anyway.\n"
			"hide_crosshair_when_aiming = 1\n"
			"\n"
			"# --- weapon pinned to the controller (EXPERIMENTAL) ---\n"
			"#\n"
			"# Source positions the viewmodel from the player's EYE, inside\n"
			"# CViewRender::SetUpView, and there is no viewmodel origin field in\n"
			"# CViewSetup to write. This finds the viewmodel entity and writes its\n"
			"# transform directly, in the window after that and before the scene is\n"
			"# drawn.\n"
			"#\n"
			"# Whether that window actually exists is UNPROVEN -- Source may\n"
			"# re-derive the transform before drawing. The heartbeat's 'viewmodel:'\n"
			"# line reports STUCK or OVERWRITTEN so the answer is in the log rather\n"
			"# than in how it looks. If OVERWRITTEN, the fix is to hook\n"
			"# CalcViewModelView the way Portal 2 VR does.\n"
			"#\n"
			"# Turn the arms off too, or they stretch from the gun to nowhere:\n"
			"# see SE1\\materials\\models\\weapons\\v_hands\\hands.vmt.\n"
			"viewmodel_follow_controller = 1\n"
			"\n"
			"# Where the model sits relative to the controller, in Source units\n"
			"# along the controller's own axes. SiN's viewmodels were authored for\n"
			"# a screen-centred weapon, so the model origin is not at the grip and\n"
			"# these will need tuning per weapon.\n"
			"viewmodel_offset_forward = -26.0\n"
			"viewmodel_offset_right = -10.0\n"
			"viewmodel_offset_up = 6.0\n"
			"\n"
			"# Rotation offsets in DEGREES, applied on top of the controller\n"
			"# orientation. A controller is not held at the angle a gun model\n"
			"# expects, and no amount of position offset fixes an angle.\n"
			"viewmodel_angle_pitch = 58.0\n"
			"viewmodel_angle_yaw = 0.0\n"
			"viewmodel_angle_roll = 0.0\n"
			"\n"
			"# Tune those three live instead of guessing. Numpad: 8/2 forward/back,\n"
			"# 4/6 left/right, 9/3 up/down, 5 reset, -/+ step size, 0 print.\n"
			"# Every change is logged as the exact cfg lines to paste back here, so\n"
			"# a tuning session ends with numbers you can keep.\n"
			"# Shares the numpad with eye_adjust_enabled -- only one may be on.\n"
			"viewmodel_adjust_enabled = 0\n"
			"\n"
			"# --- melee by gesture ---\n"
			"#\n"
			"# Swing the weapon hand down from around head height and the game plays\n"
			"# its melee attack -- a downward strike with the handle of whatever you\n"
			"# are holding, which is what SiN's own animation does. Issued as the\n"
			"# game's own '+melee' (bound to Q), asserted and released for you.\n"
			"#\n"
			"# CALIBRATE BEFORE ENABLING IT. Set melee_gesture = 0 and melee_debug = 1,\n"
			"# load a map, and swing a few times: every candidate is measured and\n"
			"# logged with the value each gate saw and whether it WOULD have fired,\n"
			"# with no chance of attacking while you do it. Then set the numbers below\n"
			"# from what your arm actually produced. The heartbeat also reports the\n"
			"# best swing seen this session.\n"
			"melee_gesture = 1\n"
			"\n"
			"# Aim a melee swing along your GAZE rather than along the weapon\n"
			"# hand.\n"
			"#\n"
			"# Source resolves the hit using the player's view angles, and aim\n"
			"# decoupling makes those the hand's -- so a downward swing lands\n"
			"# pointing at the floor. The engine also resolves the hit a few\n"
			"# frames AFTER the button goes down, so the gaze aim is held for a\n"
			"# short tail past the release or the swing reverts to the hand\n"
			"# before it connects.\n"
			"melee_aim_forward = 1\n"
			"melee_aim_tail = 0.25\n"
			"melee_debug = 0\n"
			"\n"
			"# Minimum downward speed to begin a swing, Source units per second.\n"
			"# Roughly 40 units/s is one metre per second.\n"
			"melee_min_speed = 55.0\n"
			"\n"
			"# Minimum downward distance before it fires, in units (~inches).\n"
			"# Head height to about waist is on the order of 20.\n"
			"melee_min_travel = 12.0\n"
			"\n"
			"# How high the hand must be for a swing to START, measured DOWN from the\n"
			"# headset so it follows your height rather than assuming one. -16 is about\n"
			"# chin-to-shoulder. This is the gate that rejects an arm simply being\n"
			"# lowered, so raise it towards 0 if that still fires.\n"
			"\n"
			"# Shape of the completed path, as fractions of its total length.\n"
			"# melee_min_downness is how much of it was straight down.\n"
			"# melee_min_inward is how much was ACROSS THE BODY -- a right hand\n"
			"# swinging to your left, or the mirror of that with left_handed = 1.\n"
			"# inward defaults to 0 (not required) because it is the gate most likely\n"
			"# to reject a real swing; raise it to reject arm-lowering.\n"
			"melee_min_downness = 0.55\n"
			"melee_min_inward = 0.0\n"
			"\n"
			"# A swing that has not travelled far enough within this many seconds is an\n"
			"# arm being lowered, not a swing.\n"
			"melee_max_duration = 0.7\n"
			"\n"
			"# Refractory period, seconds -- one swing must be one melee.\n"
			"melee_cooldown = 0.8\n"
			"\n"
			"# How long '+melee' is held. It has to survive long enough for the engine\n"
			"# to sample it into a usercmd, which a press and release inside a single\n"
			"# frame does not guarantee.\n"
			"melee_hold_seconds = 0.12\n"
			"\n"
			"# ---- ARCADE RELOADING ----------------------------------------------\n"
			"#\n"
			"# Drop the weapon hand to your waist and the gun reloads. While this is\n"
			"# on the reload BUTTON is unbound -- the gesture owns the command, and\n"
			"# two things driving one +reload/-reload pair would fight.\n"
			"#\n"
			"# Calibrate first: set arcade_reload = 0 and arcade_reload_debug = 1.\n"
			"# The detector then measures and logs every attempt, and says whether it\n"
			"# WOULD have reloaded, while the button keeps working normally. Read\n"
			"# 'lowest the hand reached this session' in the log and set the height\n"
			"# gate from that rather than from this default.\n"
			"#\n"
			"# NOTE: this does NOT stop the game reloading by itself when the clip\n"
			"# runs dry -- that is a weapon-script flag, not a setting. See\n"
			"# content/README.md for the one-line-per-weapon override.\n"
			"arcade_reload = 0\n"
			"arcade_reload_debug = 0\n"
			"\n"
			"\n"
			"# How long the hand must STAY there, in seconds. This is the gate that\n"
			"# separates a reload from a melee swing: a swing passes through the\n"
			"# waist and rebounds, it does not settle there.\n"
			"#\n"
			"# Lowered 0.35 -> 0.25 -> 0.12. The first cut came from a measurement\n"
			"# worth repeating: every single fire reported dwell=0.36s against a 0.35\n"
			"# gate, which says the PLAYER was waiting for the DETECTOR rather than\n"
			"# the detector catching the player. Short-dwell rejections stayed at\n"
			"# zero throughout, so there was headroom then and there is headroom now\n"
			"# -- the speed gate and the melee interlock do the discriminating, not\n"
			"# the wait.\n"
			"#\n"
			"# If reloads start firing when you merely lower your arm, read\n"
			"# 'rejected[short dwell=N]' in the log before raising this: a zero there\n"
			"# means the dwell is not what is letting them through.\n"
			"arcade_reload_dwell = 0.12\n"
			"\n"
			"# The hand must be slower than this, in units/second -- the inverse of\n"
			"# melee_min_speed. A swing's tail is still moving well above it.\n"
			"arcade_reload_max_speed = 30.0\n"
			"\n"
			"# How far ABOVE the top of the reload box the hand must rise before\n"
			"# another reload can fire. Relative to the box, so moving the box\n"
			"# carries its hysteresis with it.\n"
			"arcade_reload_rearm_above = 8.0\n"
			"\n"
			"# Refractory period after a reload, in seconds.\n"
			"arcade_reload_cooldown = 1.5\n"
			"\n"
			"# How long '+reload' is held. The engine samples key state into a\n"
			"# usercmd once per frame, so a press that goes down and up inside one\n"
			"# frame can be missed entirely.\n"
			"arcade_reload_hold_seconds = 0.12\n"
			"\n"
			"# ---- PHYSICAL CROUCHING --------------------------------------------\n"
			"#\n"
			"# Crouch in the room, crouch in the game. Measured as a FRACTION of\n"
			"# your standing height, so it fits a tall player and a short one\n"
			"# without either touching this file.\n"
			"#\n"
			"# Standing height is your head height at the last recentre, which is\n"
			"# also where 6DoF measures from -- so RECENTRE WHILE STANDING. Doing\n"
			"# it in a chair calibrates a seated player as standing, and then\n"
			"# nothing will ever trigger. The log prints the height it measured.\n"
			"#\n"
			"# The crouch button still works and is ORed with this, so a player\n"
			"# who cannot crouch physically loses nothing.\n"
			"physical_crouch = 1\n"
			"physical_crouch_debug = 0\n"
			"\n"
			"# Below this fraction of standing height you are crouching; you must\n"
			"# come back above the release fraction to stand. The gap is\n"
			"# hysteresis -- without it a head hovering on one threshold stutters\n"
			"# the duck several times a second. Release MUST be the larger of the\n"
			"# two or standing up is impossible, and startup refuses otherwise.\n"
			"physical_crouch_trigger = 0.70\n"
			"physical_crouch_release = 0.77\n"
			"\n"
			"# Cancel the engine's own duck in the view.\n"
			"#\n"
			"# Leave this ON. Crouching lowers the view TWICE otherwise: once\n"
			"# because your head really moved (6DoF) and again because the engine\n"
			"# ducked you, so a shallow crouch puts the camera at your knees.\n"
			"# With it on, the camera tracks your real head and the engine's duck\n"
			"# still shrinks your collision hull so you fit under things.\n"
			"#\n"
			"# It applies only to a duck YOU caused physically -- pressing the\n"
			"# crouch button while standing still lowers the view normally, which\n"
			"# is the whole point of the button.\n"
			"physical_crouch_compensate = 1\n"
			"\n"
			"# ---- SWIMMING ------------------------------------------------------\n"
			"#\n"
			"# Which device's pitch steers you in water.\n"
			"#\n"
			"#   movement   - follow movement_direction, whatever that is set to\n"
			"#   hmd        - swim where you look\n"
			"#   controller - swim where the off-hand controller points\n"
			"#   off        - leave the aim pitch alone\n"
			"#\n"
			"# The engine swims along your COMMAND angles' pitch. With\n"
			"# aim_source = controller those angles are the WEAPON HAND's, so\n"
			"# holding the gun level while swimming gives no upward component and\n"
			"# you sink -- which never happens on a monitor, because there the\n"
			"# view pitch IS where you look.\n"
			"#\n"
			"# NOTE the engine takes ONE pitch for movement AND shooting, so while\n"
			"# swimming your shots follow the same device as the swim. There is no\n"
			"# third value to give it. Set 'off' to keep gun-pitch aim in water and\n"
			"# sink as before.\n"
			"#\n"
			"# 'controller' uses the raw controller pitch, which on most hardware\n"
			"# points well below where your hand feels like it points -- the same\n"
			"# tilt viewmodel_angle_pitch exists to correct. Expect to swim lower\n"
			"# than you mean to until that is accounted for.\n"
			"swim_pitch = movement\n"
			"\n"
			"# ---- LASER DOT -----------------------------------------------------\n"
			"#\n"
			"# A dot in the world showing where the bullet is going.\n"
			"#\n"
			"# It is drawn on the ENGINE's eye ray, not from the gun in your hand.\n"
			"# Source fires from the player's EYE -- that is the whole reason\n"
			"# aim_convergence_distance exists -- so a dot cast from the controller\n"
			"# would sit where bullets do NOT go.\n"
			"#\n"
			"# The dot is a world-space marker rather than a moved crosshair on\n"
			"# purpose: a 2D element at a screen position sits at INFINITY in\n"
			"# stereo however near it should look, which is what shelved the\n"
			"# hand-mounted HUD. A world dot converges at its real distance.\n"
			"#\n"
			"# Distance along that ray, in units. 0 means use\n"
			"# aim_convergence_distance, which is the one range where the shot and\n"
			"# the barrel provably coincide -- so the dot doubles as a way to check\n"
			"# that the convergence is right.\n"
			"#\n"
			"# It does NOT yet stick to walls; that needs a real engine trace.\n"
			"#\n"
			"# Below skill 3 the game's autoaim can bend the shot up to 5 degrees\n"
			"# off the dot toward a nearby target. That is the game helping, not\n"
			"# the dot lying.\n"
			"laser_dot = 0\n"
			"laser_dot_distance = 0.0\n"
			"laser_dot_size = 1.2\n"
			"\n"
			"# Land it on the first thing the ray hits, rather than floating at\n"
			"# the distance above. Needs EngineTraceClient003; if that is missing\n"
			"# or its structs do not match this build, the crosshair silently goes\n"
			"# back to floating and the log line says FLOATING.\n"
			"laser_dot_trace = 1\n"
			"laser_dot_max_distance = 4096.0\n"
			"\n"
			"# Arm length in units at 250 units of range, scaled with the real\n"
			"# distance so the apparent size stays constant. `gap` is the fraction\n"
			"# of each arm left empty in the middle; 0 draws a solid plus.\n"
			"laser_dot_gap = 0.35\n"
			"\n"
			"# Draw it through walls. Off is correct -- a mark on a wall should be\n"
			"# hidden by anything in front of it. Turn on if it z-fights with the\n"
			"# surface it sits on.\n"
			"laser_dot_through_walls = 0\n"
			"\n"
			"# How many times the crosshair is submitted PER EYE PASS.\n"
			"#\n"
			"# The crosshair is now re-submitted inside each eye pass rather than\n"
			"# once per frame, because the engine draws its overlay list during the\n"
			"# first pass and clears it -- so a single submission reached one eye\n"
			"# only, and a monocular mark looks correct while your head is still\n"
			"# and swims the moment it moves. That is what the ghosting was.\n"
			"#\n"
			"# 1 is therefore the right value now: one per pass, two per frame.\n"
			"# Raising it only overdraws in the same place. Clamped to 1..4.\n"
			"laser_dot_submits = 1\n"
			"\n"
			"# Colour and OFFSET the copy drawn in each eye: eye 0 red and shifted\n"
			"# left, eye 1 green and shifted right. Diagnosis only.\n"
			"#\n"
			"# The offset matters as much as the colour. An earlier version tinted\n"
			"# without offsetting, so both copies sat in the same place and\n"
			"# whichever drew last simply covered the other -- meaning 'all one\n"
			"# colour' could not be told apart from 'only one was drawn'. Read it:\n"
			"#\n"
			"#   a red AND a green crosshair - both eye passes draw, so the\n"
			"#                                 per-pass submission works\n"
			"#   only ONE colour ever        - only one pass draws overlays at all,\n"
			"#                                 and this path cannot do stereo\n"
			"laser_dot_debug_tint = 0\n"
			"\n"
			"# Draw the CURRENT WEAPON's own reticle instead of a plain cross.\n"
			"#\n"
			"# SiN's crosshairs are font glyphs, not textures -- each weapon\n"
			"# script names a character in SE1/resource/SE1Crosshairs.ttf. The\n"
			"# outlines for those characters were read out of that font and\n"
			"# reduced to their dashes and dots, so this is the game's own shape\n"
			"# rather than an impression of it:\n"
			"#\n"
			"#   magnum          a single centre dot\n"
			"#   scattergun      four dashes, top / bottom / left / right\n"
			"#   assault rifle   those four, plus four inner ticks and a dot\n"
			"#\n"
			"# The curved wings you see either side of the crosshair in the flat\n"
			"# game are NOT part of it -- they are the ammo quickinfo, a separate\n"
			"# pair of glyphs showing clip state. Reproducing those faithfully\n"
			"# would paint an ammo gauge onto a wall at whatever range you happen\n"
			"# to be aiming, so instead each weapon gets a pair of BRACKETS that\n"
			"# echo the silhouette without pretending to mean anything:\n"
			"#\n"
			"#   magnum          tortoise-shell brackets\n"
			"#   scattergun      lenticular brackets\n"
			"#   assault rifle   double angle brackets\n"
			"#\n"
			"# The centre dot is drawn larger than the font's own, on purpose: the\n"
			"# glyph is a FILLED square and reads solid on a flat HUD, while this is\n"
			"# drawn in lines and would all but vanish at the true size -- and on\n"
			"# the magnum that dot is the whole reticle.\n"
			"#\n"
			"# 0 gives the plain four-arm cross in laser_dot_r/g/b, which is also\n"
			"# what any weapon without a reticle of its own gets.\n"
			"laser_dot_per_weapon = 1\n"
			"\n"
			"# --- seeing the aim, for zeroing the sights ---\n"
			"#\n"
			"# A crosshair shows the ANSWER without showing the MECHANISM: when\n"
			"# the dot is off you cannot tell whether the barrel points somewhere\n"
			"# else, or the shot converges at the wrong distance, or both -- and\n"
			"# those want different knobs. Tuning by dot alone is a search with\n"
			"# one bit of feedback.\n"
			"#\n"
			"# This draws the two lines that are supposed to meet:\n"
			"#\n"
			"#   GREEN  from the muzzle down the gun's own axis -- where the gun\n"
			"#          is genuinely pointing.\n"
			"#   RED    from the ENGINE'S EYE to the convergence point -- where\n"
			"#          the shot actually goes.\n"
			"#   WHITE  a small cross at the convergence point itself.\n"
			"#\n"
			"# How they relate is the whole diagnosis:\n"
			"#   parallel but offset  -> the ANGLE is right, the lateral is not\n"
			"#   crossing early/late  -> the convergence distance is off\n"
			"#   diverging            -> the angle is wrong, and NO lateral value\n"
			"#                           will fix it at more than one range\n"
			"#\n"
			"# It also prints the live tuner axis and all four values on screen,\n"
			"# so you are not tracking them from the log while wearing a headset.\n"
			"aim_debug = 0\n"
			"\n"
			"# How far the lines are drawn, in Source units.\n"
			"aim_debug_length = 600.0\n"
			"\n"
			"# ---- HAND MARKERS --------------------------------------------------\n"
			"#\n"
			"# A box where each hand is, rotated to the controller's orientation,\n"
			"# with a whisker along its forward axis.\n"
			"#\n"
			"#   off       - nothing drawn\n"
			"#   off_hand  - the empty hand only\n"
			"#   both      - both hands, for comparing one against the other\n"
			"#   auto      - DEFAULT. Off hand always; weapon hand only while you\n"
			"#               are UNARMED. With a gun in hand the gun already shows\n"
			"#               where that hand is, so a box over it is clutter --\n"
			"#               but with no gun the hand is invisible, and the whole\n"
			"#               intro is unarmed.\n"
			"#\n"
			"# This is the instrument for two-handed weapon work. The blend that\n"
			"# makes a foregrip work takes the weapon's forward from the\n"
			"# HAND-TO-HAND vector, and none of that can be tuned while the off\n"
			"# hand is invisible -- there is no way to tell 'my hand is not where\n"
			"# I think it is' from 'the blend is wrong'.\n"
			"#\n"
			"# The box is NOT given the controller-to-weapon tilt\n"
			"# (viewmodel_angle_pitch). It shows where the CONTROLLER points,\n"
			"# which is the frame a grip blend has to be reasoned about in.\n"
			"hand_marker = auto\n"
			"hand_marker_size = 0.75\n"
			"hand_marker_forward = 6.0\n"
			"\n"
			"# Where the box sits relative to the TRACKED POSE.\n"
			"#\n"
			"# OpenVR's controller pose is not the middle of the controller -- it\n"
			"# is a defined reference point sitting roughly 1.5 units along the\n"
			"# handle from the visual centre, so a box drawn straight on it hovers\n"
			"# at one end rather than around the controller.\n"
			"#\n"
			"# THE AXES ARE THE CONTROLLER'S, NOT YOURS, and they do not agree. A\n"
			"# controller's forward runs along the HANDLE, which held naturally\n"
			"# reads as up-and-down through the headset -- the same ~58 degree\n"
			"# discrepancy viewmodel_angle_pitch corrects for the weapon. So\n"
			"# 'offset_up' visibly moves the box forwards, which is bewildering\n"
			"# until the axes are named out loud:\n"
			"#\n"
			"#   offset_forward - along the WHISKER. Looks like up and down, and\n"
			"#                    is the one that centres the box on the\n"
			"#                    controller. Flip the sign if it goes the wrong\n"
			"#                    way; that depends on which end of the handle\n"
			"#                    your controller's pose sits at.\n"
			"#   offset_up      - perpendicular to the handle; reads as fore/aft.\n"
			"#   offset_right   - across the handle.\n"
			"#\n"
			"# The whisker is drawn along offset_forward's axis on purpose, so the\n"
			"# mapping is visible rather than something to find by trial.\n"
			"#\n"
			"# More than cosmetic: a two-handed grip pivots about the TRACKED\n"
			"# pose, so this offset is the gap between where the maths thinks\n"
			"# your hand is and where you feel it.\n"
			"hand_marker_offset_forward = -1.5\n"
			"hand_marker_offset_right = 0.0\n"
			"hand_marker_offset_up = 0.0\n"
			"\n"
			"# ---- TWO-HANDED WEAPONS --------------------------------------------\n"
			"#\n"
			"# Bring the off hand up to the foregrip and the gun points along the\n"
			"# line between your hands, instead of along the weapon hand's own\n"
			"# orientation. Roll still comes from the weapon wrist -- a line\n"
			"# between two points has no roll -- so canting the gun still works.\n"
			"#\n"
			"# The aim and the viewmodel both read the same answer, so they cannot\n"
			"# end up pointing in different directions. That mistake, made with\n"
			"# the weapon tilt, once put shots 58 degrees off the gun.\n"
			"#\n"
			"# Set two_handed = 0 with two_handed_debug = 1 to measure first: it\n"
			"# reports every engage and release with the distance that caused it,\n"
			"# without ever moving the gun.\n"
			"two_handed = 1\n"
			"\n"
			"# How the off hand takes hold of the gun.\n"
			"#\n"
			"#   auto    proximity alone -- bring the off hand to the\n"
			"#           foregrip and it takes hold, move it away and it\n"
			"#           lets go. Nothing to press.\n"
			"#   toggle  press the OFF hand's grip inside the zone to take\n"
			"#           hold, press again to let go.\n"
			"#   hold    the off hand's grip must stay pressed.\n"
			"#\n"
			"# The button is the OFF hand's grip -- the RIGHT hand when\n"
			"# left_handed = 1, the LEFT hand otherwise. It is free because\n"
			"# NextWeapon is bound on both grips and only the weapon hand's\n"
			"# is read.\n"
			"#\n"
			"# VIVE WANDS: keep this on 'auto'. Their grips carry Use and\n"
			"# Reload and NextWeapon is on the application menu button, so\n"
			"# there is no free grip to press.\n"
			"#\n"
			"# The engage/release radius still applies in every mode: the\n"
			"# button says you want to hold the gun, the radius says your\n"
			"# hand is somewhere that could.\n"
			"two_handed_mode = auto\n"
			"two_handed_debug = 0\n"
			"\n"
			"# How close the off hand must come to the foregrip to engage, and how\n"
			"# far it must go to let go. Release must be the larger; startup\n"
			"# widens it if not. Two radii rather than one because a hand hovering\n"
			"# on a single threshold would flicker the grip several times a\n"
			"# second -- and this one changes where the gun POINTS.\n"
			"two_handed_radius = 6.0\n"
			"two_handed_release_radius = 9.0\n"
			"\n"
			"# Where the foregrip sits, in the WEAPON HAND's own frame. Same axis\n"
			"# confusion as hand_marker: 'up' runs perpendicular to the handle and\n"
			"# reads as forward through the headset, while 'forward' runs along\n"
			"# the handle and reads as up and down.\n"
			"#\n"
			"# Turn the hand markers on to see it -- the foregrip draws as a small\n"
			"# box, dim white normally and GREEN while the grip is held.\n"
			"#\n"
			"# These are the fallback. Per-weapon values override them and are\n"
			"# absent by default, so they are NOT listed here:\n"
			"#\n"
			"#   two_handed_grip_forward_v_magnum = -3.0\n"
			"#   two_handed_grip_up_v_magnum = 3.0\n"
			"#   two_handed_grip_forward_v_assault_rifle = 0.0\n"
			"#   two_handed_grip_up_v_assault_rifle = 7.5\n"
			"#\n"
			"# Setting a _forward_ key is what marks a weapon as tuned; the others\n"
			"# are read alongside it.\n"
			"two_handed_grip_forward = 0.0\n"
			"two_handed_grip_right = 0.0\n"
			"two_handed_grip_up = 7.5\n"
			"\n"
			"# How much the line between your hands steers the gun, 0..1.\n"
			"#\n"
			"# 1 is a rifle: the off hand sits well FORWARD on the weapon, so the\n"
			"# line between your hands really is the barrel and the rear wrist\n"
			"# stops mattering.\n"
			"#\n"
			"# 0 is a pistol. A pistol's support hand wraps BESIDE and BELOW the\n"
			"# firing hand rather than in front of it, so that line points down\n"
			"# and sideways and has nothing to do with where the barrel is aimed.\n"
			"# Letting it steer sends the gun somewhere you never pointed. The\n"
			"# support hand is still detected -- it just gets no vote on where the\n"
			"# gun points.\n"
			"#\n"
			"# In between is a weighted average of the two directions, which is\n"
			"# the right shape for anything gripped between those extremes.\n"
			"#\n"
			"# Per-weapon override, absent by default:\n"
			"#   two_handed_blend_v_magnum = 0.0\n"
			"two_handed_blend = 1.0\n"
			"\n"
			"# ---- BODY ZONES ----------------------------------------------------\n"
			"#\n"
			"# Melee, the holsters and arcade reload all ask the same question --\n"
			"# is the weapon hand at a particular place on your body -- so they are\n"
			"# all boxes, with a centre and a size on each axis.\n"
			"#\n"
			"#   forward  + is the way you are facing\n"
			"#   lateral  + is to your RIGHT\n"
			"#   up       + is above your head, so body zones are NEGATIVE\n"
			"#\n"
			"# All measured from the HMD, in units (~inches), with only your head's\n"
			"# YAW applied -- so a zone is the same reach whatever your height, and\n"
			"# looking down does not rotate your holsters out from under your hand.\n"
			"#\n"
			"# Sizes are FULL width, not half-extents. A size of 0 on any axis\n"
			"# switches that zone off.\n"
			"#\n"
			"# Set zone_boxes_visible = 1 to draw them: purple = hip holster\n"
			"# (pistol), blue = left holster, orange = right holster, green = melee\n"
			"# start, red = reload. Turn it off when you are done -- it is a\n"
			"# positioning aid, not a feature.\n"
			"zone_boxes_visible = 0\n"
			"\n"
			"# Position the zones live, from inside the headset, on the numpad.\n"
			"# Turns the boxes on by itself if they are off.\n"
			"#\n"
			"#   1      next zone            7      position <-> size\n"
			"#   8 / 2  forward / back       9 / 3  up / down\n"
			"#   4 / 6  left / right         .      SNAP to the weapon hand\n"
			"#   5 5    reset this zone      0      dump all five as cfg lines\n"
			"#   - / +  smaller / larger step\n"
			"#\n"
			"# The one to use is `.` -- hold your hand where the zone should be\n"
			"# and press it, and the box centres there. Nudging is for adjusting\n"
			"# afterwards. Press 0 when you are happy and paste the printed lines\n"
			"# back into this file; nothing is saved automatically.\n"
			"#\n"
			"# Shares the numpad with eye_adjust_enabled and\n"
			"# viewmodel_adjust_enabled -- at most one of the three may be on,\n"
			"# and startup refuses and says so rather than letting one key do\n"
			"# two jobs.\n"
			"zone_adjust_enabled = 0\n"
			"\n"
			"# --- holster zone geometry: BUILT IN, PER HAND ---\n"
			"#\n"
			"# The holster zones sit in body space and left_handed swaps which\n"
			"# controller reaches for them, so one set of numbers cannot serve\n"
			"# both hands -- the same-side and cross-body reaches trade places,\n"
			"# and the boxes are not even the same shape as each other.\n"
			"#\n"
			"# The mod carries a profile for EACH hand and left_handed picks\n"
			"# between them, so THERE ARE DELIBERATELY NO zone_holster_* LINES\n"
			"# HERE. That is the fix for a real bug: this file used to ship the\n"
			"# right-handed numbers, and a cfg value overrides the built-in\n"
			"# profile -- so setting left_handed = 1 on a fresh install left you\n"
			"# reaching for right-handed zones and every draw missed.\n"
			"#\n"
			"# To tune your own, set zone_adjust_enabled = 1, move the boxes with\n"
			"# the numpad, and copy the zone_holster_* lines the log prints into\n"
			"# this file. They will then override the built-in profile.\n"
			"#\n"
			"# If you do that and later flip left_handed, DELETE those lines --\n"
			"# they are tuned for one hand, and the built-in profile for the\n"
			"# other cannot apply while they are here.\n"
			"# was start height >= -16, unbounded -> up -16..+40, wide sideways\n"
			"zone_melee_forward = 0.0\n"
			"zone_melee_lateral = 0.0\n"
			"zone_melee_up = 12.0\n"
			"zone_melee_size_forward = 80.0\n"
			"zone_melee_size_lateral = 80.0\n"
			"zone_melee_size_up = 56.0\n"
			"\n"
			"# was height <= -26, unbounded -> -26..-56\n"
			"zone_reload_forward = 4.0\n"
			"zone_reload_lateral = 4.0\n"
			"zone_reload_up = -37.0\n"
			"zone_reload_size_forward = 56.0\n"
			"zone_reload_size_lateral = 30.0\n"
			"zone_reload_size_up = 34.0\n"
			"\n"
			"# --- viewmodel animations ---\n"
			"#\n"
			"# The gun is pinned to your controller, but the game still plays its own\n"
			"# arm animations on it -- so it performs motions your arm did not make.\n"
			"# The clearest case is melee: you swing the controller, the gesture fires\n"
			"# +melee, and then the gun swings AGAIN.\n"
			"#\n"
			"# Suppression is by sequence NAME, resolved per model at runtime, because\n"
			"# the three viewmodels number their sequences differently -- 'reload' is 7\n"
			"# on the magnum and the assault rifle but the scattergun has no sequence of\n"
			"# that name at all.\n"
			"#\n"
			"# Log every sequence change first. It names the animation, its index, the\n"
			"# category it was put in and whether it was suppressed, which is the only\n"
			"# way to see what the game is really playing and when.\n"
			"viewmodel_anim_log = 0\n"
			"\n"
			"# The doubled melee swing. The natural companion to melee_gesture.\n"
			"viewmodel_anim_suppress_melee = 1\n"
			"\n"
			"# Reload -- the gun leaves your hand and comes back.\n"
			"# CAVEAT: if a reload sound is an animation EVENT rather than a call in\n"
			"# weapon code, cancelling the sequence takes the sound with it. Listen for\n"
			"# that before deciding it is an improvement.\n"
			"viewmodel_anim_suppress_reload = 1\n"
			"\n"
			"# draw / holster on a weapon switch. Off by default -- it is brief, and it\n"
			"# is the only feedback that the switch happened.\n"
			"viewmodel_anim_suppress_draw = 0\n"
			"\n"
			"# The scripted 'weapon lowered' state used around dialogue. Off by default:\n"
			"# this is the game deliberately taking the weapon away, and overriding it\n"
			"# may read as the gun ignoring a cutscene.\n"
			"viewmodel_anim_suppress_lower = 0\n"
			"\n"
			"# Firing animations are NEVER suppressed. Muzzle flash and shell ejection\n"
			"# hang off them, and the point is to remove what you did not do yourself.\n"
			"\n"
			"\n"
			"# --- per-weapon viewmodel offsets ---\n"
			"#\n"
			"# The three viewmodels are authored with different origins, so one set of\n"
			"# offsets cannot seat all three in the hand -- measured on hardware, the\n"
			"# magnum sat right while the scattergun was too far back and the assault\n"
			"# rifle slightly too far forward.\n"
			"#\n"
			"# A weapon with NO keys here uses the global viewmodel_offset_* values, so\n"
			"# an existing tuned config keeps working and only the guns that need their\n"
			"# own numbers get them. Angles stay global: they are how a hand holds a gun,\n"
			"# which does not change with the gun.\n"
			"#\n"
			"# Tune them live -- viewmodel_adjust_enabled edits whichever weapon you are\n"
			"# holding and prints that weapon's lines to paste back here.\n"
			"# viewmodel_offset_forward_v_magnum = -26.0\n"
			"# viewmodel_offset_right_v_magnum = -10.0\n"
			"# viewmodel_offset_up_v_magnum = 6.0\n"
			"# viewmodel_offset_forward_v_assault_rifle = -26.0\n"
			"# viewmodel_offset_right_v_assault_rifle = -10.0\n"
			"# viewmodel_offset_up_v_assault_rifle = 6.0\n"
			"# viewmodel_offset_forward_v_scattergun = -26.0\n"
			"# viewmodel_offset_right_v_scattergun = -10.0\n"
			"# viewmodel_offset_up_v_scattergun = 6.0\n"
			"\n"
			"# Diagnostic for the firing flicker: prints the viewmodel transform before\n"
			"# and after EACH eye pass for this many frames. Our write happens once per\n"
			"# frame but the engine re-poses the viewmodel inside every pass, so if the\n"
			"# two eyes disagree the gun appears to flash sideways. 0 is off.\n"
			"viewmodel_trace_eye_passes = 0\n"
			"\n"
			"# Entity layout diagnosis, read-only. Runs this many times, a few seconds\n"
			"# apart, and reports every field of the viewmodel entity that currently\n"
			"# holds the abs origin -- as a Vector, and as the translation column of a\n"
			"# 3x4 matrix.\n"
			"#\n"
			"# Why it exists: the eye-pass trace showed m_vecAbsOrigin perfectly stable\n"
			"# while the gun was visibly flickering. Both can only be true if the field\n"
			"# being measured is not the one that DRAWS. Source renders from\n"
			"# m_rgflCoordinateFrame, a cached matrix we never write, so our position\n"
			"# only takes effect when something else happens to rebuild it.\n"
			"viewmodel_diagnose = 0\n"
			"\n"
			"# Dump every weapon-shaped renderable AT THE INSTANT OF FIRING, this many\n"
			"# times. Read-only.\n"
			"#\n"
			"# The second gun model is SiN's C_Weapon* entity carrying a v_ model of its\n"
			"# own at eye height -- now handled by weapon_entity_pin / weapon_entity_hide.\n"
			"# It did NOT carry the muzzle flash; that was the engine rescaling it, see\n"
			"# viewmodel_fov_match. Kept because enumerating renderables at the instant\n"
			"# of firing is still the right tool if a third copy ever turns up.\n"
			"# This runs on the frame a firing animation begins.\n"
			"viewmodel_diagnose_fire = 0\n"
			"\n"
			"# Write m_rgflCoordinateFrame -- the matrix3x4 the RENDER actually reads --\n"
			"# as well as the origin fields. THIS IS THE FIX FOR THE FIRING FLICKER.\n"
			"#\n"
			"# Writing m_vecAbsOrigin was never enough. Source draws a client entity from\n"
			"# that matrix, which CalcAbsolutePosition() rebuilds from the LOCAL origin,\n"
			"# and the engine's CalcViewModelView rewrites local every frame. Measured on\n"
			"# hardware across five samples: m_vecAbsOrigin matched what we wrote 5/5,\n"
			"# while the local origin and the matrix matched only 2/5 -- and on exactly\n"
			"# the same samples as each other. So on the other 3 the gun was drawn where\n"
			"# the engine put it. Firing widens that window, which is why the flicker\n"
			"# tracked the rate of fire.\n"
			"#\n"
			"# The offset is re-verified at runtime before a single byte is written: the\n"
			"# matrix built from the entity's own abs origin and angles must match what is\n"
			"# already there, to all twelve floats. If it never does, nothing is written\n"
			"# and the log says so.\n"
			"viewmodel_write_coordinate_frame = 1\n"
			"\n"
			"# --- THE VIEWMODEL GHOSTING FIX ---\n"
			"#\n"
			"# Substitute the gun's pose at the engine's OWN SetLocalOrigin/SetLocalAngles\n"
			"# instead of writing the transform fields directly.\n"
			"#\n"
			"# Why it matters: SetLocalOrigin does not just store a value, it INVALIDATES\n"
			"# THE BONE CACHE. Four earlier fixes wrote m_vecOrigin, m_vecAbsOrigin, the\n"
			"# angles and m_rgflCoordinateFrame directly. Every one of them landed, and\n"
			"# none changed what was drawn, because the bones kept being rebuilt from the\n"
			"# engine's pose. Measured: our transform was intact 3114 times out of 3114\n"
			"# immediately before the gun was drawn, while it visibly flickered.\n"
			"#\n"
			"# CBaseViewModel::CalcViewModelView (client.dll+0x9C970) calls both setters\n"
			"# VIRTUALLY, at vtable +0xE8 and +0xF0 -- slots 58 and 60 -- so this needs no\n"
			"# signature scan and no trampoline. Confirmed live: 8946 substitutions in\n"
			"# 8946 calls.\n"
			"#\n"
			"# Substituting at the setter also discards the engine's bob, lag and shake,\n"
			"# which are applied to the locals beforehand. In VR that is what you want.\n"
			"#\n"
			"# Turn off only to reproduce the old ghosting.\n"
			"viewmodel_hook_setters = 1\n"
			"\n"
			"# --- gun collision ---\n"
			"#\n"
			"# Stop the gun phasing through walls. Two sweeps, using the same\n"
			"# engine trace the head collision uses:\n"
			"#\n"
			"#   BODY    eye -> hand, so you cannot push the gun sideways\n"
			"#           through a wall.\n"
			"#   BARREL  hand -> muzzle along the gun's own axis, because\n"
			"#           pulling the grip clear still leaves the barrel buried.\n"
			"#\n"
			"# The correction is a RIGID move of the whole model, so the gun\n"
			"# never separates from the hand holding it.\n"
			"viewmodel_collide = 1\n"
			"\n"
			"# Half-extent of the sweep hull, in Source units. A 0 here would be\n"
			"# a bare ray and would let the model's width clip through; too big\n"
			"# and the gun shies away from doorframes it would have cleared.\n"
			"viewmodel_collide_radius = 2.0\n"
			"\n"
			"# Hand-to-muzzle distance kept clear, in Source units. Roughly the\n"
			"# length of gun in front of your hand -- shorter for the magnum,\n"
			"# longer for the rifle; 12 is a middle that suits all three. Set 0\n"
			"# to collide the grip only and let the barrel clip.\n"
			"viewmodel_collide_barrel = 12.0\n"
			"\n"
			"# How much of the barrel's penetration is actually taken off the\n"
			"# gun, and the reason this is not 1.0.\n"
			"#\n"
			"# 1.0 pulls the barrel fully clear of the wall. It is geometrically\n"
			"# correct and it reads as the gun being SHOVED at your face. Below 1\n"
			"# the barrel is allowed to stay a little buried in exchange for the\n"
			"# gun not lunging -- a straight trade of accuracy for comfort.\n"
			"#\n"
			"# The retreat is also EASED over about 60 ms rather than applied\n"
			"# outright, in both directions, so a trace that flickers between\n"
			"# blocked and clear across one frame does not twitch the gun.\n"
			"#\n"
			"# Applies to the barrel only. The eye-to-hand clamp is a hard\n"
			"# constraint and is never softened: doing that would let the gun\n"
			"# enter the wall and then ease back out.\n"
			"viewmodel_collide_response = 0.35\n"
			"\n"
			"# Maximum angle, in degrees, the barrel may be SWUNG AWAY from a\n"
			"# surface before the retreat has to take over. 0 disables\n"
			"# deflection and leaves only the clamp and the retreat.\n"
			"#\n"
			"# Deflection is preferred because it keeps the gun WHERE YOUR HAND\n"
			"# IS -- it pivots about the grip and declines to put the barrel\n"
			"# through the wall. Retreat cannot do that: it slides the model\n"
			"# away from the controller, which is what read as a shove.\n"
			"#\n"
			"# It cannot cover everything either. Pushed square into a wall\n"
			"# there is no sideways to swing to, so the retreat stays as the\n"
			"# fallback and a re-trace on the deflected axis decides how much of\n"
			"# each is actually needed.\n"
			"#\n"
			"# The angle is driven by HOW DEEP the barrel is: nothing at the\n"
			"# surface, and the full value with the whole barrel buried. So it\n"
			"# builds as you push in and unwinds as you pull out.\n"
			"#\n"
			"# Raise it for a gun that gets further out of the way; lower it if\n"
			"# the model tilting bothers you more than the barrel clipping.\n"
			"viewmodel_collide_deflect = 60.0\n"
			"\n"
			"# Shape of that ramp against depth. 1.0 is linear. Above 1 leaves\n"
			"# the gun nearly alone when the barrel has only grazed a surface\n"
			"# and swings harder the further it is pushed through -- lower this\n"
			"# toward 1.0 if you want the deflection to start sooner, raise it\n"
			"# if a light touch is tilting the gun more than you like.\n"
			"viewmodel_collide_deflect_curve = 1.5\n"
			"\n"
			"# --- the weapon entity's own copy of the gun ---\n"
			"#\n"
			"# --- the weapon entity's own copy of the gun ---\n"
			"#\n"
			"# SiN's C_Weapon* entities each carry a v_ VIEWMODEL as their model and sit\n"
			"# at a player-relative (-4.9, 0, 35.7) -- eye height. On a monitor that lands\n"
			"# on top of the real viewmodel, so nobody ever sees two guns.\n"
			"#\n"
			"# This has been blamed for two defects and caused neither: not the viewmodel\n"
			"# ghosting (that was the bone cache -- see viewmodel_hook_setters) and not\n"
			"# the misaligned muzzle flash (that was the engine rescaling it -- see\n"
			"# viewmodel_fov_match). Turning these on is still worth it, because a second\n"
			"# gun floating at eye height is worth removing on its own.\n"
			"#\n"
			"# pin  gives it the viewmodel's transform, so effects anchored to it land at\n"
			"#      the real gun. It calls the entity's OWN SetLocalOrigin rather than\n"
			"#      writing the fields, because an attachment comes from bones and only\n"
			"#      the setter invalidates the bone cache -- the same lesson that fixed\n"
			"#      the ghosting.\n"
			"# hide sets EF_NODRAW so the duplicate mesh stops drawing. Pin without hide\n"
			"#      leaves two identical models in one place, which z-fights.\n"
			"#\n"
			"# Both default ON. Read the 'weapon entity' line in sinvr.log to see which\n"
			"# path actually ran: pins climbing through the SETTER is the working case,\n"
			"# 'raw fallback' means the vtable slots did not resolve and the flash will\n"
			"# still be wrong.\n"
			"weapon_entity_pin = 1\n"
			"weapon_entity_hide = 1\n"
			"\n"
			"\n"
			"# --- holster zones ---\n"
			"#\n"
			"# Reach to a place on your body and press GRIP on the weapon hand to draw\n"
			"# that weapon. Outside every zone the grip does nothing by default --\n"
			"# see holster_grip_cycles below.\n"
			"#\n"
			"# Slots are Source's slotN commands -- the number keys. In SiN: 1 pistol,\n"
			"# 2 shotgun, 3 assault rifle.\n"
			"holster_zones = 1\n"
			"holster_left_slot = 3\n"
			"holster_right_slot = 2\n"
			"holster_hip_slot = 1\n"
			"\n"
			"# Whether a grip OUTSIDE every zone still cycles weapons.\n"
			"#\n"
			"# OFF by default. It used to be on -- the grip kept its old `invnext`\n"
			"# meaning wherever a zone did not claim it, so nothing was lost. In play\n"
			"# that turned out to be the wrong trade: a grip gets pressed incidentally\n"
			"# all the time, steadying a two-handed gun or just resting a hand, and\n"
			"# every one of those swapped your weapon.\n"
			"#\n"
			"# Safe to have off for a countable reason rather than a hopeful one: SiN\n"
			"# has three guns and there are three zones, so the cycle can reach nothing\n"
			"# a zone does not already cover. Note that Previous Weapon is bound to\n"
			"# nothing on any controller profile, so this is the only cycle there is --\n"
			"# set it to 1 if you ever carry a weapon that no zone is assigned to.\n"
			"holster_grip_cycles = 0\n"
			"\n"
			"# Zone geometry, in units, measured from the HEAD so it follows your height\n"
			"# rather than assuming one. Sideways reach for a side zone; below hip_height\n"
			"# is the hip zone whichever side it is on; above side_height_max is a raised\n"
			"# hand rather than a holster.\n"
			"\n"
			"# Log the measured hand position on EVERY grip press, hit or miss. This is\n"
			"# how the three numbers above get set from a real reach instead of guessed.\n"
			"holster_debug = 0\n"
			"\n"
			"# ==== the menu cursor, drawn by the mod in D3D9 ====\n"
			"#\n"
			"# The only cursor that can appear over an in-game PAUSE menu.\n"
			"#\n"
			"# Everything else was tried and each died to the engine's draw\n"
			"# order rather than to anything wrong with the cursor: the OS\n"
			"# cursor is composited onto the DESKTOP and never into the\n"
			"# backbuffer we submit; debug overlays are drawn BEFORE VGUI, so\n"
			"# they sit behind the menu, and are not drawn at all while a\n"
			"# pause menu is up.\n"
			"#\n"
			"# This one is drawn at the end of each eye pass, after the engine\n"
			"# has finished and before the image is sent to the headset.\n"
			"#\n"
			"# 0 falls back to the old world-space overlay cross, which works\n"
			"# on the main menu and is invisible when paused.\n"
			"menu_cursor_d3d9 = 1\n"
			"\n"
			"# Arm half-length and bar thickness, in BACKBUFFER pixels -- not\n"
			"# window pixels. Those differ (1800x2124 against 1200x1416), so a\n"
			"# size that looks right in the window would be 1.5x out.\n"
			"menu_cursor_size = 16\n"
			"menu_cursor_thickness = 4\n"
			"\n"
			"# Dark border around the cross, in pixels. 0 turns it off.\n"
			"# Not decoration: SiN's menus are light-on-dark on some screens\n"
			"# and dark-on-light on others, and a single-colour cursor\n"
			"# disappears against one of them.\n"
			"menu_cursor_outline = 2\n"
			"\n"
			"# Cursor colour, 0-255 each.\n"
			"menu_cursor_r = 255\n"
			"menu_cursor_g = 240\n"
			"menu_cursor_b = 64\n"
			"\n"
			"# ==== render resolution, taken from the headset ====\n"
			"#\n"
			"# READ BY sinvr_launcher.exe, not by the mod -- the window size\n"
			"# has to be decided before the game starts, and the mod only\n"
			"# reports what it ended up with.\n"
			"#\n"
			"# The game renders the scene once per eye into its own\n"
			"# backbuffer, and the eye surfaces match that backbuffer\n"
			"# exactly -- so THE GAME WINDOW SIZE *IS* THE PER-EYE RENDER\n"
			"# RESOLUTION. There is no separate knob inside the mod.\n"
			"#\n"
			"# With this on, the launcher asks the OpenVR runtime for the\n"
			"# recommended per-eye size and appends -w/-h itself. That makes\n"
			"# SteamVR's own resolution slider the control, and it works on\n"
			"# any headset without anyone knowing its numbers.\n"
			"#\n"
			"# An explicit -w/-h in Steam's launch options always wins.\n"
			"vr_auto_resolution = 1\n"
			"\n"
			"# Multiplier on the runtime's recommendation. 1.0 renders what\n"
			"# SteamVR asks for.\n"
			"#\n"
			"# THIS IS THE FIRST PERFORMANCE LEVER. The scene is rendered\n"
			"# TWICE per frame, so pixel cost scales with the square of this:\n"
			"# 0.75 is a little over half the pixels of 1.0.\n"
			"#\n"
			"# Getting the ASPECT right matters more than the size. The\n"
			"# engine frustum is extended to cover the render target's aspect\n"
			"# and cropped back per eye, so a mismatched aspect still looks\n"
			"# correct -- it just renders pixels that are thrown away before\n"
			"# submission. Taking it from the runtime keeps it exact at any\n"
			"# scale.\n"
			"vr_resolution_scale = 1.0\n"
			"\n"
			"# Render at the headset's resolution even when that is bigger than\n"
			"# your MONITOR.\n"
			"#\n"
			"# The engine refuses a window larger than the display, so the\n"
			"# launcher normally shrinks the render to fit the desktop. That is\n"
			"# a real loss on a small monitor: a 1080p desktop cannot reach a\n"
			"# modern headset's native per-eye resolution in either axis, so\n"
			"# this is not extra sharpness, it is the sharpness you already\n"
			"# paid for.\n"
			"#\n"
			"# Set to 1 and the launcher stops clamping, and tells our DXVK\n"
			"# build to report a display large enough that the engine accepts\n"
			"# the window. The refusal is D3D9's, which is ours; the override is\n"
			"# only ever as large as the run actually asks for.\n"
			"#\n"
			"# The game WINDOW will then be bigger than your screen and hang off\n"
			"# the edge. That is expected, costs nothing, and does not affect\n"
			"# what you see in the headset.\n"
			"#\n"
			"# How much to render is still SteamVR's resolution slider, via\n"
			"# GetRecommendedRenderTargetSize -- so raise it for sharpness or\n"
			"# lower it for frame rate. Above the desktop size that slider\n"
			"# currently does nothing; this is what makes it live again. Watch\n"
			"# `stereo frame cost` in sinvr.log before settling on a number.\n"
			"vr_allow_oversize_window = 1\n"
			"\n"
			"# --- image quality, applied through DXVK ---\n"
			"#\n"
			"# READ BY THE LAUNCHER, not the mod: these become DXVK_CONFIG\n"
			"# entries before the game starts, so they only take effect on a\n"
			"# launch through sinvr_launcher.exe.\n"
			"#\n"
			"# WHY THEY EXIST. Measured 2026-09-05: the scene really is rendered\n"
			"# into a 2444x2392 MSAA=4 target at native resolution, so MSAA is\n"
			"# working and the shimmer that survives it is not geometric\n"
			"# aliasing. MSAA anti-aliases polygon SILHOUETTES only -- an\n"
			"# alpha-tested edge (grates, railings, ladders, fences, and SiN is\n"
			"# full of them) lives INSIDE the polygon and gets one sample no\n"
			"# matter how many the polygon gets.\n"
			"#\n"
			"# Runs the pixel shader per SAMPLE rather than per pixel, which is\n"
			"# what actually anti-aliases those edges, and specular sparkle with\n"
			"# them. Requires MSAA to be on in the game's video settings -- it\n"
			"# does nothing without it.\n"
			"#\n"
			"# DXVK's own warning: \"significant performance cost\". This is the\n"
			"# expensive one; try it and watch the heartbeat's frame count.\n"
			"dxvk_sample_rate_shading = 1\n"
			"\n"
			"# Force anisotropic filtering, 1-16. 0 leaves the game's own choice\n"
			"# alone. A different axis of the same complaint: texture shimmer at\n"
			"# grazing angles, which neither MSAA nor sample-rate shading\n"
			"# touches. Cheap, unlike the setting above.\n"
			"dxvk_anisotropy = 16\n"
			"\n"
			"# --- clicking menu items below the bottom of your monitor ---\n"
			"#\n"
			"# vr_allow_oversize_window makes the game window TALLER than your\n"
			"# display, and the OS cursor cannot go past the desktop edge. So the\n"
			"# VR pointer draws where you aim but the click lands short -- the\n"
			"# heartbeat measures it as \"OS clamped on N move(s), worst NNN px\".\n"
			"#\n"
			"# Clipping the mouse to the window does not help: that constrains a\n"
			"# cursor, it cannot extend the desktop. The only fix is to stop\n"
			"# using the OS cursor and set VGUI's own, which has no desktop to be\n"
			"# clamped to.\n"
			"#\n"
			"# OFF BY DEFAULT ON PURPOSE. This once crashed the game at launch:\n"
			"# its slot check passed on garbage and vgui2.dll slot 5 was called\n"
			"# where it is not SetCursorPos. The idea was never disproved -- the\n"
			"# CHECK was wrong -- but it is not proven either.\n"
			"#\n"
			"# If the game crashes at launch with this on, set it back to 0.\n"
			"vgui_cursor = 0\n"
			"\n"
			"# A ceiling on the per-eye render HEIGHT, aspect preserved.\n"
			"#\n"
			"# SteamVR's resolution slider is the supersampling control, and\n"
			"# a slider left somewhere ambitious asks for a lot: 3988x3904\n"
			"# per eye is 31 megapixels a frame at 90 Hz. On this project\n"
			"# that did not merely run slowly -- it took the HEADSET down\n"
			"# before the game was even playable.\n"
			"#\n"
			"# Set this to your headset's native per-eye height to say\n"
			"# 'render native, never supersample'. The launcher prints the\n"
			"# size it settles on at every start, so you can read the right\n"
			"# number off one run with the SteamVR slider at 100%.\n"
			"#\n"
			"# 0 disables the cap. This is INDEPENDENT of\n"
			"# vr_allow_oversize_window: that one is about the monitor,\n"
			"# this one is about what the GPU and the headset can take.\n"
			"vr_max_render_height = 0\n"
			"\n"
			"# ==== 6DoF collision ====\n"
			"#\n"
			"# Stop the head being leaned or stepped through walls.\n"
			"#\n"
			"# The 6DoF offset moves the CAMERA only -- the player entity\n"
			"# stays where the engine put it -- so nothing in the game\n"
			"# collides it. positional_max_offset bounds how FAR it can go\n"
			"# and knows nothing about what is in the way, which is why a\n"
			"# lean into a wall put the view inside it.\n"
			"#\n"
			"# This sweeps the offset against the world from the engine's own\n"
			"# eye and stops it short of whatever it hits.\n"
			"# ==== scripted cameras (cutscenes) ====\n"
			"#\n"
			"# SiN drives the camera during scripted sequences -- the intro on a\n"
			"# new game is the first one you will meet.\n"
			"#\n"
			"#   head    the script is IGNORED for view rotation. Your head keeps\n"
			"#           control. Comfortable, and your forward is still correct\n"
			"#           when the scene ends.\n"
			"#   engine  the script MOVES your view, as the flat game does. More\n"
			"#           cinematic, and a well-known nausea trigger in VR.\n"
			"#\n"
			"# A teleport or a spawn-angle change is still adopted either way --\n"
			"# those are one frame, a cutscene is hundreds, and the mod tells\n"
			"# them apart by duration rather than by size.\n"
			"cutscene_camera = head\n"
			"\n"
			"# Turn the player to face a cutscene ONCE, as it begins.\n"
			"#\n"
			"# A scene that does not rotate your view can start with you\n"
			"# facing away from it entirely. This aligns your body to the\n"
			"# camera's heading at the moment it starts, so you begin looking\n"
			"# the right way -- and because it turns the WORLD rather than\n"
			"# your view, it costs nothing in comfort, exactly as a recentre\n"
			"# does.\n"
			"#\n"
			"# Yaw only. If the scene is overhead you still have to look up,\n"
			"# which is what the arrow below is for.\n"
			"cutscene_recenter = 1\n"
			"\n"
			"# Let FL_FROZEN alone identify a cutscene.\n"
			"#\n"
			"# The game sets FL_FROZEN when it takes control away from you,\n"
			"# which is what every point_viewcontrol does. This used to count\n"
			"# only if the camera was also ROTATING -- so a scene shot from a\n"
			"# FIXED camera was never detected at all, and you were left facing\n"
			"# wherever the scene teleported you rather than at the scene.\n"
			"#\n"
			"# 0 restores that, in case something that is not a cutscene freezes\n"
			"# the player and turning is affected.\n"
			"cutscene_frozen_latch = 1\n"
			"\n"
			"# Trace the rotation chain, one line per frame while you TURN.\n"
			"#\n"
			"# For diagnosing judder. A 5-second heartbeat samples one frame in\n"
			"# 450 and cannot see it at all. The view yaw is exactly\n"
			"#     view = body + hmd + offset\n"
			"# so this prints all of them and whichever is not smooth is the\n"
			"# fault. Off by default -- it writes a lot of lines.\n"
			"# Take the head pose from the compositor, not from our own guess.\n"
			"#\n"
			"# WaitGetPoses already hands us poses the runtime predicted for\n"
			"# the exact frame being drawn. The old path re-queried tracking\n"
			"# with an interval computed from time-since-vsync, which only\n"
			"# holds still if the app runs on the display cadence -- this one\n"
			"# does not, so the interval jittered by up to a whole frame.\n"
			"#\n"
			"# Prediction error follows VELOCITY, so it was invisible while\n"
			"# walking and obvious while turning the head. 0 restores the old\n"
			"# behaviour for comparison.\n"
			"compositor_poses = 1\n"
			"\n"
			"# Stop weapon recoil from turning you.\n"
			"#\n"
			"# The engine moves its own yaw while a weapon kicks, and the body\n"
			"# yaw treats any engine movement as you turning -- so firing swings\n"
			"# the world under a head that did not move. Measured with the head\n"
			"# held still: +-0.03 deg of head motion against 0.23..0.95 deg of\n"
			"# body yaw per sample. Worst on the assault rifle, which kicks\n"
			"# hardest and longest.\n"
			"#\n"
			"# The viewkick is known by name (m_vecPunchAngle), so its share of\n"
			"# the delta is subtracted rather than guessed at. What is left is\n"
			"# real: a teleport, a script, a push. This is NOT the same as\n"
			"# recoil_compensation, which only changes where the BULLET goes.\n"
			"recoil_view_lock = 1\n"
			"\n"
			"# How big an engine turn can be and still count as recoil, and how\n"
			"# long after the trigger to keep listening for it.\n"
			"#\n"
			"# The rifle's kick is +-1.0 deg of YAW per shot (kickRampMin/Max in\n"
			"# weapon_assault_rifle.txt), so the cap is well above it and well\n"
			"# below a teleport -- a real engine move during a firefight is\n"
			"# still adopted. The tail exists because the kick is applied\n"
			"# server-side and the rotated angles arrive a frame or two after\n"
			"# the button.\n"
			"recoil_view_lock_max_degrees = 3.0\n"
			"recoil_view_lock_tail_ms = 250\n"
			"\n"
			"rotation_trace = 0\n"
			"rotation_trace_frames = 900\n"
			"\n"
			"# Leave in-game screens and mirrors alone.\n"
			"#\n"
			"# A monitor in the world is rendered by a camera into a texture,\n"
			"# and that texture is mapped onto a surface. The 3D comes from the\n"
			"# SURFACE -- the picture itself must be identical in both eyes,\n"
			"# because a real screen does not show each eye a different image.\n"
			"#\n"
			"# Giving those cameras the eye's own frustum rendered the contents\n"
			"# from two different viewpoints, and the eyes could not fuse them.\n"
			"# They are told apart by aspect: the world and the viewmodel share\n"
			"# the headset's target, an RT camera has its texture's shape.\n"
			"stereo_skip_rt_projections = 1\n"
			"stereo_rt_aspect_tolerance = 0.02\n"
			"\n"
			"# Point you at the scene once, after a LEVEL CHANGE.\n"
			"#\n"
			"# OFF by default. A normal level load already spawns you facing\n"
			"# the right way, so turning you there is wrong -- it fired on an\n"
			"# ordinary load into se1_intro03 and rotated the player 112 deg\n"
			"# for no reason. The in-map TELEPORT trigger below is the one that\n"
			"# earns its keep; this is kept only for a map hand-off that really\n"
			"# does drop you facing backwards.\n"
			"#\n"
			"# SiN's 'white flash into a new scene' is a LEVEL CHANGE, and it\n"
			"# can spawn you facing away from what the scene is about -- the\n"
			"# intro01 -> intro02 hand-off does exactly that. The game does not\n"
			"# freeze you there, so the cutscene detectors never see it.\n"
			"#\n"
			"# Aims at whatever the engine's camera is looking at, so there is\n"
			"# no per-scene number and it works on any map. On an ordinary\n"
			"# spawn the camera already IS your view, so it turns you by ~0 and\n"
			"# nothing happens -- which is why it is safe to leave on.\n"
			"map_turn_to_face = 0\n"
			"\n"
			"# How long to wait after the load before reading the camera. The\n"
			"# view has not settled on the first frames of a level.\n"
			"map_turn_delay_frames = 45\n"
			"\n"
			"# Do not bother turning for less than this. A few degrees is an\n"
			"# ordinary spawn, not a scene facing the wrong way.\n"
			"map_turn_min_degrees = 25\n"
			"\n"
			"# Also point you at the scene after an in-map TELEPORT.\n"
			"#\n"
			"# SiN's flash-cuts during the car ride are not cutscenes and not\n"
			"# level changes -- the game moves you to a room built elsewhere in\n"
			"# the same map, plays the scene, and moves you back. Nothing\n"
			"# freezes you and no camera is driven, so the only evidence is\n"
			"# that your position jumped.\n"
			"map_turn_on_teleport = 1\n"
			"\n"
			"# How big a one-frame jump counts as a teleport. Normal motion is\n"
			"# about 7 units a frame even in the car; the real jumps are\n"
			"# thousands, so there is a lot of room between the two.\n"
			"map_turn_teleport_units = 400\n"
			"\n"
			"# Drop the HUD anchor for this long around a transition.\n"
			"#\n"
			"# The game's fade to white is drawn through the same hook as the\n"
			"# HUD and in the SAME ortho window, so with hud_anchor = body it\n"
			"# gets moved and shrunk with the HUD and appears off to one side\n"
			"# instead of covering your view. It cannot be told apart at the\n"
			"# hook, so it is separated by time instead: every flash in the\n"
			"# game coincides with a level change or a teleport, both of which\n"
			"# are detected.\n"
			"#\n"
			"# Losing the anchor briefly costs nothing -- the HUD is not\n"
			"# readable through a white-out anyway. 0 disables the workaround.\n"
			"hud_anchor_transition_ms = 2500\n"
			"\n"
			"# Spot a fade by how many times the HUD's ortho window is drawn.\n"
			"#\n"
			"# The fade cannot be told from the HUD by its extents or its z --\n"
			"# both were measured and both are identical -- but the HUD draws\n"
			"# that window about twice per eye pass and a fade takes it to four.\n"
			"# Above this count the anchor is dropped so the wash covers the\n"
			"# view. This catches fades the transition timer above cannot,\n"
			"# including the fade-OUT that happens before a teleport.\n"
			"#\n"
			"# MEASURED AND OFF BY DEFAULT. The distribution turned out to be\n"
			"# 0:3131 3:768 4:26 5:774 -- the HUD's own normal draw is 3 OR 5\n"
			"# calls a pass, so there is no threshold above the HUD and below\n"
			"# the fade. Setting it to 3 dropped the anchor on nearly every\n"
			"# pass that drew the HUD and made it flicker. A simple count\n"
			"# cannot separate them; leave this at 0 unless that changes.\n"
			"#\n"
			"# Set it from the distribution the log prints, not by feel. 0 off.\n"
			"hud_anchor_fade_calls = 0\n"
			"\n"
			"# Point at the scene for as long as you are looking away from it.\n"
			"#\n"
			"# The direction is the cutscene camera's own, taken from\n"
			"# CViewSetup, and converted to a screen direction with vectors --\n"
			"# NOT by comparing pitch against yaw, which points the wrong way\n"
			"# whenever the target is steeply above or below you.\n"
			"cutscene_look_arrow = 1\n"
			"\n"
			"# How far off-target you may be and still count as looking at the\n"
			"# scene, as a fraction of the way to the edge of your view.\n"
			"#\n"
			"# Derived from the frustum actually being rendered rather than\n"
			"# fixed in degrees, because a VR eye covers something like 110\n"
			"# degrees -- a subject 30 degrees off centre is still plainly\n"
			"# visible, and demanding you centre it is just nagging. 0.7 means\n"
			"# anywhere in the middle 70% of the way to the edge is fine.\n"
			"#\n"
			"# Raise it to be told less often, lower it to be told sooner.\n"
			"cutscene_look_arrow_fov = 0.85\n"
			"\n"
			"# Floors in degrees, used only before the frustum is known. The\n"
			"# FOV-derived values above are normally larger and win.\n"
			"cutscene_look_arrow_hide = 25.0\n"
			"cutscene_look_arrow_fade = 60.0\n"
			"\n"
			"# Chevron size in BACKBUFFER pixels, and pulse rate in Hz.\n"
			"cutscene_look_arrow_size = 80\n"
			"cutscene_look_arrow_pulse = 1.4\n"
			"\n"
			"# Modifier required by the mod's keyboard shortcuts:\n"
			"#\n"
			"#   Ctrl+Alt+F9    recentre\n"
			"#   Ctrl+Alt+F10   VR camera on / off\n"
			"#   Ctrl+Alt+F11   roll on / off\n"
			"#\n"
			"# They need one because every recording tool binds something in\n"
			"# the F9-F12 range -- Steam, GeForce Experience, OBS, the Windows\n"
			"# Game Bar. Without a modifier, pressing record also turned the VR\n"
			"# camera off, which looks exactly like tracking failing.\n"
			"#\n"
			"# Modifiers must match EXACTLY, so Ctrl+Alt+F11 does not fire on\n"
			"# Ctrl+F11, and the game must be the focused window.\n"
			"#\n"
			"# ctrl_alt (default), ctrl, alt, ctrl_shift, none\n"
			"hotkey_modifier = ctrl_alt\n"
			"\n"
			"positional_collide = 1\n"
			"\n"
			"# Half-size of the box that is swept, in Source units. A HULL,\n"
			"# not a ray: a point slips through the corner of a doorframe it\n"
			"# technically missed and the near plane then clips into geometry\n"
			"# the trace called clear.\n"
			"#\n"
			"# Raise it if the view still clips into walls; lower it if you\n"
			"# are stopped short of gaps you should fit through.\n"
			"positional_collide_radius = 6.0\n"
			"\n"
			"# How far short of the surface to stop, as a fraction of the\n"
			"# swept distance. A camera exactly ON a plane still z-fights\n"
			"# through the near plane.\n"
			"positional_collide_backoff = 0.05\n"
			"\n"
			"# The sound a holster draw plays, as a soundscript name from\n"
			"# SE1/scripts/game_sounds_ui.txt. Empty = silent.\n"
			"#\n"
			"# It needs one because the ENGINE does not give it one: slotN under\n"
			"# hud_fastswitch switches instantly and returns before its own\n"
			"# EmitSound, while the invnext it replaced always emits. So a draw\n"
			"# was silent and a cycle was not.\n"
			"#\n"
			"# Player.WeaponSelected          the confirm sound (wpn_select.wav)\n"
			"# Player.WeaponSelectionMoveSlot what invnext plays (wpn_moveselect.wav)\n"
			"holster_draw_sound = Player.WeaponSelected\n"
			"\n",
			f );
		fclose( f );
	}

	std::vector<std::pair<std::string, std::string>> m_entries;
	bool m_loaded = false;
	int m_upgradedFrom = 0;
};

} // namespace sinvr
